// Code generated by ent, DO NOT EDIT.

package team

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/SAP/astonish/ent/team/chatsessionevent"
	"github.com/SAP/astonish/ent/team/session"
	"github.com/google/uuid"
)

// ChatSessionEvent is the model entity for the ChatSessionEvent schema.
type ChatSessionEvent struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// ChatSessionID holds the value of the "chat_session_id" field.
	ChatSessionID string `json:"chat_session_id,omitempty"`
	// Seq holds the value of the "seq" field.
	Seq int64 `json:"seq,omitempty"`
	// EventType holds the value of the "event_type" field.
	EventType string `json:"event_type,omitempty"`
	// Payload holds the value of the "payload" field.
	Payload map[string]interface{} `json:"payload,omitempty"`
	// ProducerPod holds the value of the "producer_pod" field.
	ProducerPod string `json:"producer_pod,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ChatSessionEventQuery when eager-loading is set.
	Edges        ChatSessionEventEdges `json:"edges"`
	selectValues sql.SelectValues
}

// ChatSessionEventEdges holds the relations/edges for other nodes in the graph.
type ChatSessionEventEdges struct {
	// Session holds the value of the session edge.
	Session *Session `json:"session,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// SessionOrErr returns the Session value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ChatSessionEventEdges) SessionOrErr() (*Session, error) {
	if e.Session != nil {
		return e.Session, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: session.Label}
	}
	return nil, &NotLoadedError{edge: "session"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ChatSessionEvent) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case chatsessionevent.FieldPayload:
			values[i] = new([]byte)
		case chatsessionevent.FieldSeq:
			values[i] = new(sql.NullInt64)
		case chatsessionevent.FieldChatSessionID, chatsessionevent.FieldEventType, chatsessionevent.FieldProducerPod:
			values[i] = new(sql.NullString)
		case chatsessionevent.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case chatsessionevent.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ChatSessionEvent fields.
func (_m *ChatSessionEvent) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case chatsessionevent.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case chatsessionevent.FieldChatSessionID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field chat_session_id", values[i])
			} else if value.Valid {
				_m.ChatSessionID = value.String
			}
		case chatsessionevent.FieldSeq:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field seq", values[i])
			} else if value.Valid {
				_m.Seq = value.Int64
			}
		case chatsessionevent.FieldEventType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field event_type", values[i])
			} else if value.Valid {
				_m.EventType = value.String
			}
		case chatsessionevent.FieldPayload:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field payload", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.Payload); err != nil {
					return fmt.Errorf("unmarshal field payload: %w", err)
				}
			}
		case chatsessionevent.FieldProducerPod:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field producer_pod", values[i])
			} else if value.Valid {
				_m.ProducerPod = value.String
			}
		case chatsessionevent.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ChatSessionEvent.
// This includes values selected through modifiers, order, etc.
func (_m *ChatSessionEvent) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QuerySession queries the "session" edge of the ChatSessionEvent entity.
func (_m *ChatSessionEvent) QuerySession() *SessionQuery {
	return NewChatSessionEventClient(_m.config).QuerySession(_m)
}

// Update returns a builder for updating this ChatSessionEvent.
// Note that you need to call ChatSessionEvent.Unwrap() before calling this method if this ChatSessionEvent
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *ChatSessionEvent) Update() *ChatSessionEventUpdateOne {
	return NewChatSessionEventClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the ChatSessionEvent entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *ChatSessionEvent) Unwrap() *ChatSessionEvent {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("team: ChatSessionEvent is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *ChatSessionEvent) String() string {
	var builder strings.Builder
	builder.WriteString("ChatSessionEvent(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("chat_session_id=")
	builder.WriteString(_m.ChatSessionID)
	builder.WriteString(", ")
	builder.WriteString("seq=")
	builder.WriteString(fmt.Sprintf("%v", _m.Seq))
	builder.WriteString(", ")
	builder.WriteString("event_type=")
	builder.WriteString(_m.EventType)
	builder.WriteString(", ")
	builder.WriteString("payload=")
	builder.WriteString(fmt.Sprintf("%v", _m.Payload))
	builder.WriteString(", ")
	builder.WriteString("producer_pod=")
	builder.WriteString(_m.ProducerPod)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// ChatSessionEvents is a parsable slice of ChatSessionEvent.
type ChatSessionEvents []*ChatSessionEvent
