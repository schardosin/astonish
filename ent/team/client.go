// Code generated by ent, DO NOT EDIT.

package team

import (
	"context"
	"errors"
	"fmt"
	"log"
	"reflect"

	"github.com/google/uuid"
	"github.com/schardosin/astonish/ent/team/migrate"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"github.com/schardosin/astonish/ent/team/app"
	"github.com/schardosin/astonish/ent/team/appstate"
	"github.com/schardosin/astonish/ent/team/chatsessionevent"
	"github.com/schardosin/astonish/ent/team/credential"
	"github.com/schardosin/astonish/ent/team/drillreport"
	"github.com/schardosin/astonish/ent/team/fleetmailboxmessage"
	"github.com/schardosin/astonish/ent/team/fleetmonitorstate"
	"github.com/schardosin/astonish/ent/team/fleetplan"
	"github.com/schardosin/astonish/ent/team/fleetrunstate"
	"github.com/schardosin/astonish/ent/team/fleetsetupdraft"
	"github.com/schardosin/astonish/ent/team/fleetsetupprofile"
	"github.com/schardosin/astonish/ent/team/fleettask"
	"github.com/schardosin/astonish/ent/team/fleettemplate"
	"github.com/schardosin/astonish/ent/team/flow"
	"github.com/schardosin/astonish/ent/team/mcpserver"
	"github.com/schardosin/astonish/ent/team/memory"
	"github.com/schardosin/astonish/ent/team/networkpolicy"
	"github.com/schardosin/astonish/ent/team/sandboxsession"
	"github.com/schardosin/astonish/ent/team/scheduledjob"
	"github.com/schardosin/astonish/ent/team/session"
	"github.com/schardosin/astonish/ent/team/sessionevent"
	"github.com/schardosin/astonish/ent/team/setting"
	"github.com/schardosin/astonish/ent/team/skill"
	"github.com/schardosin/astonish/ent/team/skillfile"
	"github.com/schardosin/astonish/ent/team/teamauditlog"
)

// Client is the client that holds all ent builders.
type Client struct {
	config
	// Schema is the client for creating, migrating and dropping schema.
	Schema *migrate.Schema
	// App is the client for interacting with the App builders.
	App *AppClient
	// AppState is the client for interacting with the AppState builders.
	AppState *AppStateClient
	// ChatSessionEvent is the client for interacting with the ChatSessionEvent builders.
	ChatSessionEvent *ChatSessionEventClient
	// Credential is the client for interacting with the Credential builders.
	Credential *CredentialClient
	// DrillReport is the client for interacting with the DrillReport builders.
	DrillReport *DrillReportClient
	// FleetMailboxMessage is the client for interacting with the FleetMailboxMessage builders.
	FleetMailboxMessage *FleetMailboxMessageClient
	// FleetMonitorState is the client for interacting with the FleetMonitorState builders.
	FleetMonitorState *FleetMonitorStateClient
	// FleetPlan is the client for interacting with the FleetPlan builders.
	FleetPlan *FleetPlanClient
	// FleetRunState is the client for interacting with the FleetRunState builders.
	FleetRunState *FleetRunStateClient
	// FleetSetupDraft is the client for interacting with the FleetSetupDraft builders.
	FleetSetupDraft *FleetSetupDraftClient
	// FleetSetupProfile is the client for interacting with the FleetSetupProfile builders.
	FleetSetupProfile *FleetSetupProfileClient
	// FleetTask is the client for interacting with the FleetTask builders.
	FleetTask *FleetTaskClient
	// FleetTemplate is the client for interacting with the FleetTemplate builders.
	FleetTemplate *FleetTemplateClient
	// Flow is the client for interacting with the Flow builders.
	Flow *FlowClient
	// McpServer is the client for interacting with the McpServer builders.
	McpServer *McpServerClient
	// Memory is the client for interacting with the Memory builders.
	Memory *MemoryClient
	// NetworkPolicy is the client for interacting with the NetworkPolicy builders.
	NetworkPolicy *NetworkPolicyClient
	// SandboxSession is the client for interacting with the SandboxSession builders.
	SandboxSession *SandboxSessionClient
	// ScheduledJob is the client for interacting with the ScheduledJob builders.
	ScheduledJob *ScheduledJobClient
	// Session is the client for interacting with the Session builders.
	Session *SessionClient
	// SessionEvent is the client for interacting with the SessionEvent builders.
	SessionEvent *SessionEventClient
	// Setting is the client for interacting with the Setting builders.
	Setting *SettingClient
	// Skill is the client for interacting with the Skill builders.
	Skill *SkillClient
	// SkillFile is the client for interacting with the SkillFile builders.
	SkillFile *SkillFileClient
	// TeamAuditLog is the client for interacting with the TeamAuditLog builders.
	TeamAuditLog *TeamAuditLogClient
}

// NewClient creates a new client configured with the given options.
func NewClient(opts ...Option) *Client {
	client := &Client{config: newConfig(opts...)}
	client.init()
	return client
}

func (c *Client) init() {
	c.Schema = migrate.NewSchema(c.driver)
	c.App = NewAppClient(c.config)
	c.AppState = NewAppStateClient(c.config)
	c.ChatSessionEvent = NewChatSessionEventClient(c.config)
	c.Credential = NewCredentialClient(c.config)
	c.DrillReport = NewDrillReportClient(c.config)
	c.FleetMailboxMessage = NewFleetMailboxMessageClient(c.config)
	c.FleetMonitorState = NewFleetMonitorStateClient(c.config)
	c.FleetPlan = NewFleetPlanClient(c.config)
	c.FleetRunState = NewFleetRunStateClient(c.config)
	c.FleetSetupDraft = NewFleetSetupDraftClient(c.config)
	c.FleetSetupProfile = NewFleetSetupProfileClient(c.config)
	c.FleetTask = NewFleetTaskClient(c.config)
	c.FleetTemplate = NewFleetTemplateClient(c.config)
	c.Flow = NewFlowClient(c.config)
	c.McpServer = NewMcpServerClient(c.config)
	c.Memory = NewMemoryClient(c.config)
	c.NetworkPolicy = NewNetworkPolicyClient(c.config)
	c.SandboxSession = NewSandboxSessionClient(c.config)
	c.ScheduledJob = NewScheduledJobClient(c.config)
	c.Session = NewSessionClient(c.config)
	c.SessionEvent = NewSessionEventClient(c.config)
	c.Setting = NewSettingClient(c.config)
	c.Skill = NewSkillClient(c.config)
	c.SkillFile = NewSkillFileClient(c.config)
	c.TeamAuditLog = NewTeamAuditLogClient(c.config)
}

type (
	// config is the configuration for the client and its builder.
	config struct {
		// driver used for executing database requests.
		driver dialect.Driver
		// debug enable a debug logging.
		debug bool
		// log used for logging on debug mode.
		log func(...any)
		// hooks to execute on mutations.
		hooks *hooks
		// interceptors to execute on queries.
		inters *inters
	}
	// Option function to configure the client.
	Option func(*config)
)

// newConfig creates a new config for the client.
func newConfig(opts ...Option) config {
	cfg := config{log: log.Println, hooks: &hooks{}, inters: &inters{}}
	cfg.options(opts...)
	return cfg
}

// options applies the options on the config object.
func (c *config) options(opts ...Option) {
	for _, opt := range opts {
		opt(c)
	}
	if c.debug {
		c.driver = dialect.Debug(c.driver, c.log)
	}
}

// Debug enables debug logging on the ent.Driver.
func Debug() Option {
	return func(c *config) {
		c.debug = true
	}
}

// Log sets the logging function for debug mode.
func Log(fn func(...any)) Option {
	return func(c *config) {
		c.log = fn
	}
}

// Driver configures the client driver.
func Driver(driver dialect.Driver) Option {
	return func(c *config) {
		c.driver = driver
	}
}

// Open opens a database/sql.DB specified by the driver name and
// the data source name, and returns a new client attached to it.
// Optional parameters can be added for configuring the client.
func Open(driverName, dataSourceName string, options ...Option) (*Client, error) {
	switch driverName {
	case dialect.MySQL, dialect.Postgres, dialect.SQLite:
		drv, err := sql.Open(driverName, dataSourceName)
		if err != nil {
			return nil, err
		}
		return NewClient(append(options, Driver(drv))...), nil
	default:
		return nil, fmt.Errorf("unsupported driver: %q", driverName)
	}
}

// ErrTxStarted is returned when trying to start a new transaction from a transactional client.
var ErrTxStarted = errors.New("team: cannot start a transaction within a transaction")

// Tx returns a new transactional client. The provided context
// is used until the transaction is committed or rolled back.
func (c *Client) Tx(ctx context.Context) (*Tx, error) {
	if _, ok := c.driver.(*txDriver); ok {
		return nil, ErrTxStarted
	}
	tx, err := newTx(ctx, c.driver)
	if err != nil {
		return nil, fmt.Errorf("team: starting a transaction: %w", err)
	}
	cfg := c.config
	cfg.driver = tx
	return &Tx{
		ctx:                 ctx,
		config:              cfg,
		App:                 NewAppClient(cfg),
		AppState:            NewAppStateClient(cfg),
		ChatSessionEvent:    NewChatSessionEventClient(cfg),
		Credential:          NewCredentialClient(cfg),
		DrillReport:         NewDrillReportClient(cfg),
		FleetMailboxMessage: NewFleetMailboxMessageClient(cfg),
		FleetMonitorState:   NewFleetMonitorStateClient(cfg),
		FleetPlan:           NewFleetPlanClient(cfg),
		FleetRunState:       NewFleetRunStateClient(cfg),
		FleetSetupDraft:     NewFleetSetupDraftClient(cfg),
		FleetSetupProfile:   NewFleetSetupProfileClient(cfg),
		FleetTask:           NewFleetTaskClient(cfg),
		FleetTemplate:       NewFleetTemplateClient(cfg),
		Flow:                NewFlowClient(cfg),
		McpServer:           NewMcpServerClient(cfg),
		Memory:              NewMemoryClient(cfg),
		NetworkPolicy:       NewNetworkPolicyClient(cfg),
		SandboxSession:      NewSandboxSessionClient(cfg),
		ScheduledJob:        NewScheduledJobClient(cfg),
		Session:             NewSessionClient(cfg),
		SessionEvent:        NewSessionEventClient(cfg),
		Setting:             NewSettingClient(cfg),
		Skill:               NewSkillClient(cfg),
		SkillFile:           NewSkillFileClient(cfg),
		TeamAuditLog:        NewTeamAuditLogClient(cfg),
	}, nil
}

// BeginTx returns a transactional client with specified options.
func (c *Client) BeginTx(ctx context.Context, opts *sql.TxOptions) (*Tx, error) {
	if _, ok := c.driver.(*txDriver); ok {
		return nil, errors.New("ent: cannot start a transaction within a transaction")
	}
	tx, err := c.driver.(interface {
		BeginTx(context.Context, *sql.TxOptions) (dialect.Tx, error)
	}).BeginTx(ctx, opts)
	if err != nil {
		return nil, fmt.Errorf("ent: starting a transaction: %w", err)
	}
	cfg := c.config
	cfg.driver = &txDriver{tx: tx, drv: c.driver}
	return &Tx{
		ctx:                 ctx,
		config:              cfg,
		App:                 NewAppClient(cfg),
		AppState:            NewAppStateClient(cfg),
		ChatSessionEvent:    NewChatSessionEventClient(cfg),
		Credential:          NewCredentialClient(cfg),
		DrillReport:         NewDrillReportClient(cfg),
		FleetMailboxMessage: NewFleetMailboxMessageClient(cfg),
		FleetMonitorState:   NewFleetMonitorStateClient(cfg),
		FleetPlan:           NewFleetPlanClient(cfg),
		FleetRunState:       NewFleetRunStateClient(cfg),
		FleetSetupDraft:     NewFleetSetupDraftClient(cfg),
		FleetSetupProfile:   NewFleetSetupProfileClient(cfg),
		FleetTask:           NewFleetTaskClient(cfg),
		FleetTemplate:       NewFleetTemplateClient(cfg),
		Flow:                NewFlowClient(cfg),
		McpServer:           NewMcpServerClient(cfg),
		Memory:              NewMemoryClient(cfg),
		NetworkPolicy:       NewNetworkPolicyClient(cfg),
		SandboxSession:      NewSandboxSessionClient(cfg),
		ScheduledJob:        NewScheduledJobClient(cfg),
		Session:             NewSessionClient(cfg),
		SessionEvent:        NewSessionEventClient(cfg),
		Setting:             NewSettingClient(cfg),
		Skill:               NewSkillClient(cfg),
		SkillFile:           NewSkillFileClient(cfg),
		TeamAuditLog:        NewTeamAuditLogClient(cfg),
	}, nil
}

// Debug returns a new debug-client. It's used to get verbose logging on specific operations.
//
//	client.Debug().
//		App.
//		Query().
//		Count(ctx)
func (c *Client) Debug() *Client {
	if c.debug {
		return c
	}
	cfg := c.config
	cfg.driver = dialect.Debug(c.driver, c.log)
	client := &Client{config: cfg}
	client.init()
	return client
}

// Close closes the database connection and prevents new queries from starting.
func (c *Client) Close() error {
	return c.driver.Close()
}

// Use adds the mutation hooks to all the entity clients.
// In order to add hooks to a specific client, call: `client.Node.Use(...)`.
func (c *Client) Use(hooks ...Hook) {
	for _, n := range []interface{ Use(...Hook) }{
		c.App, c.AppState, c.ChatSessionEvent, c.Credential, c.DrillReport,
		c.FleetMailboxMessage, c.FleetMonitorState, c.FleetPlan, c.FleetRunState,
		c.FleetSetupDraft, c.FleetSetupProfile, c.FleetTask, c.FleetTemplate, c.Flow,
		c.McpServer, c.Memory, c.NetworkPolicy, c.SandboxSession, c.ScheduledJob,
		c.Session, c.SessionEvent, c.Setting, c.Skill, c.SkillFile, c.TeamAuditLog,
	} {
		n.Use(hooks...)
	}
}

// Intercept adds the query interceptors to all the entity clients.
// In order to add interceptors to a specific client, call: `client.Node.Intercept(...)`.
func (c *Client) Intercept(interceptors ...Interceptor) {
	for _, n := range []interface{ Intercept(...Interceptor) }{
		c.App, c.AppState, c.ChatSessionEvent, c.Credential, c.DrillReport,
		c.FleetMailboxMessage, c.FleetMonitorState, c.FleetPlan, c.FleetRunState,
		c.FleetSetupDraft, c.FleetSetupProfile, c.FleetTask, c.FleetTemplate, c.Flow,
		c.McpServer, c.Memory, c.NetworkPolicy, c.SandboxSession, c.ScheduledJob,
		c.Session, c.SessionEvent, c.Setting, c.Skill, c.SkillFile, c.TeamAuditLog,
	} {
		n.Intercept(interceptors...)
	}
}

// Mutate implements the ent.Mutator interface.
func (c *Client) Mutate(ctx context.Context, m Mutation) (Value, error) {
	switch m := m.(type) {
	case *AppMutation:
		return c.App.mutate(ctx, m)
	case *AppStateMutation:
		return c.AppState.mutate(ctx, m)
	case *ChatSessionEventMutation:
		return c.ChatSessionEvent.mutate(ctx, m)
	case *CredentialMutation:
		return c.Credential.mutate(ctx, m)
	case *DrillReportMutation:
		return c.DrillReport.mutate(ctx, m)
	case *FleetMailboxMessageMutation:
		return c.FleetMailboxMessage.mutate(ctx, m)
	case *FleetMonitorStateMutation:
		return c.FleetMonitorState.mutate(ctx, m)
	case *FleetPlanMutation:
		return c.FleetPlan.mutate(ctx, m)
	case *FleetRunStateMutation:
		return c.FleetRunState.mutate(ctx, m)
	case *FleetSetupDraftMutation:
		return c.FleetSetupDraft.mutate(ctx, m)
	case *FleetSetupProfileMutation:
		return c.FleetSetupProfile.mutate(ctx, m)
	case *FleetTaskMutation:
		return c.FleetTask.mutate(ctx, m)
	case *FleetTemplateMutation:
		return c.FleetTemplate.mutate(ctx, m)
	case *FlowMutation:
		return c.Flow.mutate(ctx, m)
	case *McpServerMutation:
		return c.McpServer.mutate(ctx, m)
	case *MemoryMutation:
		return c.Memory.mutate(ctx, m)
	case *NetworkPolicyMutation:
		return c.NetworkPolicy.mutate(ctx, m)
	case *SandboxSessionMutation:
		return c.SandboxSession.mutate(ctx, m)
	case *ScheduledJobMutation:
		return c.ScheduledJob.mutate(ctx, m)
	case *SessionMutation:
		return c.Session.mutate(ctx, m)
	case *SessionEventMutation:
		return c.SessionEvent.mutate(ctx, m)
	case *SettingMutation:
		return c.Setting.mutate(ctx, m)
	case *SkillMutation:
		return c.Skill.mutate(ctx, m)
	case *SkillFileMutation:
		return c.SkillFile.mutate(ctx, m)
	case *TeamAuditLogMutation:
		return c.TeamAuditLog.mutate(ctx, m)
	default:
		return nil, fmt.Errorf("team: unknown mutation type %T", m)
	}
}

// AppClient is a client for the App schema.
type AppClient struct {
	config
}

// NewAppClient returns a client for the App from the given config.
func NewAppClient(c config) *AppClient {
	return &AppClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `app.Hooks(f(g(h())))`.
func (c *AppClient) Use(hooks ...Hook) {
	c.hooks.App = append(c.hooks.App, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `app.Intercept(f(g(h())))`.
func (c *AppClient) Intercept(interceptors ...Interceptor) {
	c.inters.App = append(c.inters.App, interceptors...)
}

// Create returns a builder for creating a App entity.
func (c *AppClient) Create() *AppCreate {
	mutation := newAppMutation(c.config, OpCreate)
	return &AppCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of App entities.
func (c *AppClient) CreateBulk(builders ...*AppCreate) *AppCreateBulk {
	return &AppCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *AppClient) MapCreateBulk(slice any, setFunc func(*AppCreate, int)) *AppCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &AppCreateBulk{err: fmt.Errorf("calling to AppClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*AppCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &AppCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for App.
func (c *AppClient) Update() *AppUpdate {
	mutation := newAppMutation(c.config, OpUpdate)
	return &AppUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *AppClient) UpdateOne(_m *App) *AppUpdateOne {
	mutation := newAppMutation(c.config, OpUpdateOne, withApp(_m))
	return &AppUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *AppClient) UpdateOneID(id uuid.UUID) *AppUpdateOne {
	mutation := newAppMutation(c.config, OpUpdateOne, withAppID(id))
	return &AppUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for App.
func (c *AppClient) Delete() *AppDelete {
	mutation := newAppMutation(c.config, OpDelete)
	return &AppDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *AppClient) DeleteOne(_m *App) *AppDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *AppClient) DeleteOneID(id uuid.UUID) *AppDeleteOne {
	builder := c.Delete().Where(app.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &AppDeleteOne{builder}
}

// Query returns a query builder for App.
func (c *AppClient) Query() *AppQuery {
	return &AppQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeApp},
		inters: c.Interceptors(),
	}
}

// Get returns a App entity by its id.
func (c *AppClient) Get(ctx context.Context, id uuid.UUID) (*App, error) {
	return c.Query().Where(app.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *AppClient) GetX(ctx context.Context, id uuid.UUID) *App {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *AppClient) Hooks() []Hook {
	return c.hooks.App
}

// Interceptors returns the client interceptors.
func (c *AppClient) Interceptors() []Interceptor {
	return c.inters.App
}

func (c *AppClient) mutate(ctx context.Context, m *AppMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&AppCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&AppUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&AppUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&AppDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("team: unknown App mutation op: %q", m.Op())
	}
}

// AppStateClient is a client for the AppState schema.
type AppStateClient struct {
	config
}

// NewAppStateClient returns a client for the AppState from the given config.
func NewAppStateClient(c config) *AppStateClient {
	return &AppStateClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `appstate.Hooks(f(g(h())))`.
func (c *AppStateClient) Use(hooks ...Hook) {
	c.hooks.AppState = append(c.hooks.AppState, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `appstate.Intercept(f(g(h())))`.
func (c *AppStateClient) Intercept(interceptors ...Interceptor) {
	c.inters.AppState = append(c.inters.AppState, interceptors...)
}

// Create returns a builder for creating a AppState entity.
func (c *AppStateClient) Create() *AppStateCreate {
	mutation := newAppStateMutation(c.config, OpCreate)
	return &AppStateCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of AppState entities.
func (c *AppStateClient) CreateBulk(builders ...*AppStateCreate) *AppStateCreateBulk {
	return &AppStateCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *AppStateClient) MapCreateBulk(slice any, setFunc func(*AppStateCreate, int)) *AppStateCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &AppStateCreateBulk{err: fmt.Errorf("calling to AppStateClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*AppStateCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &AppStateCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for AppState.
func (c *AppStateClient) Update() *AppStateUpdate {
	mutation := newAppStateMutation(c.config, OpUpdate)
	return &AppStateUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *AppStateClient) UpdateOne(_m *AppState) *AppStateUpdateOne {
	mutation := newAppStateMutation(c.config, OpUpdateOne, withAppState(_m))
	return &AppStateUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *AppStateClient) UpdateOneID(id uuid.UUID) *AppStateUpdateOne {
	mutation := newAppStateMutation(c.config, OpUpdateOne, withAppStateID(id))
	return &AppStateUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for AppState.
func (c *AppStateClient) Delete() *AppStateDelete {
	mutation := newAppStateMutation(c.config, OpDelete)
	return &AppStateDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *AppStateClient) DeleteOne(_m *AppState) *AppStateDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *AppStateClient) DeleteOneID(id uuid.UUID) *AppStateDeleteOne {
	builder := c.Delete().Where(appstate.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &AppStateDeleteOne{builder}
}

// Query returns a query builder for AppState.
func (c *AppStateClient) Query() *AppStateQuery {
	return &AppStateQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeAppState},
		inters: c.Interceptors(),
	}
}

// Get returns a AppState entity by its id.
func (c *AppStateClient) Get(ctx context.Context, id uuid.UUID) (*AppState, error) {
	return c.Query().Where(appstate.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *AppStateClient) GetX(ctx context.Context, id uuid.UUID) *AppState {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *AppStateClient) Hooks() []Hook {
	return c.hooks.AppState
}

// Interceptors returns the client interceptors.
func (c *AppStateClient) Interceptors() []Interceptor {
	return c.inters.AppState
}

func (c *AppStateClient) mutate(ctx context.Context, m *AppStateMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&AppStateCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&AppStateUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&AppStateUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&AppStateDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("team: unknown AppState mutation op: %q", m.Op())
	}
}

// ChatSessionEventClient is a client for the ChatSessionEvent schema.
type ChatSessionEventClient struct {
	config
}

// NewChatSessionEventClient returns a client for the ChatSessionEvent from the given config.
func NewChatSessionEventClient(c config) *ChatSessionEventClient {
	return &ChatSessionEventClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `chatsessionevent.Hooks(f(g(h())))`.
func (c *ChatSessionEventClient) Use(hooks ...Hook) {
	c.hooks.ChatSessionEvent = append(c.hooks.ChatSessionEvent, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `chatsessionevent.Intercept(f(g(h())))`.
func (c *ChatSessionEventClient) Intercept(interceptors ...Interceptor) {
	c.inters.ChatSessionEvent = append(c.inters.ChatSessionEvent, interceptors...)
}

// Create returns a builder for creating a ChatSessionEvent entity.
func (c *ChatSessionEventClient) Create() *ChatSessionEventCreate {
	mutation := newChatSessionEventMutation(c.config, OpCreate)
	return &ChatSessionEventCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of ChatSessionEvent entities.
func (c *ChatSessionEventClient) CreateBulk(builders ...*ChatSessionEventCreate) *ChatSessionEventCreateBulk {
	return &ChatSessionEventCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *ChatSessionEventClient) MapCreateBulk(slice any, setFunc func(*ChatSessionEventCreate, int)) *ChatSessionEventCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &ChatSessionEventCreateBulk{err: fmt.Errorf("calling to ChatSessionEventClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*ChatSessionEventCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &ChatSessionEventCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for ChatSessionEvent.
func (c *ChatSessionEventClient) Update() *ChatSessionEventUpdate {
	mutation := newChatSessionEventMutation(c.config, OpUpdate)
	return &ChatSessionEventUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *ChatSessionEventClient) UpdateOne(_m *ChatSessionEvent) *ChatSessionEventUpdateOne {
	mutation := newChatSessionEventMutation(c.config, OpUpdateOne, withChatSessionEvent(_m))
	return &ChatSessionEventUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *ChatSessionEventClient) UpdateOneID(id uuid.UUID) *ChatSessionEventUpdateOne {
	mutation := newChatSessionEventMutation(c.config, OpUpdateOne, withChatSessionEventID(id))
	return &ChatSessionEventUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for ChatSessionEvent.
func (c *ChatSessionEventClient) Delete() *ChatSessionEventDelete {
	mutation := newChatSessionEventMutation(c.config, OpDelete)
	return &ChatSessionEventDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *ChatSessionEventClient) DeleteOne(_m *ChatSessionEvent) *ChatSessionEventDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *ChatSessionEventClient) DeleteOneID(id uuid.UUID) *ChatSessionEventDeleteOne {
	builder := c.Delete().Where(chatsessionevent.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &ChatSessionEventDeleteOne{builder}
}

// Query returns a query builder for ChatSessionEvent.
func (c *ChatSessionEventClient) Query() *ChatSessionEventQuery {
	return &ChatSessionEventQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeChatSessionEvent},
		inters: c.Interceptors(),
	}
}

// Get returns a ChatSessionEvent entity by its id.
func (c *ChatSessionEventClient) Get(ctx context.Context, id uuid.UUID) (*ChatSessionEvent, error) {
	return c.Query().Where(chatsessionevent.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *ChatSessionEventClient) GetX(ctx context.Context, id uuid.UUID) *ChatSessionEvent {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// QuerySession queries the session edge of a ChatSessionEvent.
func (c *ChatSessionEventClient) QuerySession(_m *ChatSessionEvent) *SessionQuery {
	query := (&SessionClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := _m.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(chatsessionevent.Table, chatsessionevent.FieldID, id),
			sqlgraph.To(session.Table, session.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, chatsessionevent.SessionTable, chatsessionevent.SessionColumn),
		)
		fromV = sqlgraph.Neighbors(_m.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// Hooks returns the client hooks.
func (c *ChatSessionEventClient) Hooks() []Hook {
	return c.hooks.ChatSessionEvent
}

// Interceptors returns the client interceptors.
func (c *ChatSessionEventClient) Interceptors() []Interceptor {
	return c.inters.ChatSessionEvent
}

func (c *ChatSessionEventClient) mutate(ctx context.Context, m *ChatSessionEventMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&ChatSessionEventCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&ChatSessionEventUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&ChatSessionEventUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&ChatSessionEventDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("team: unknown ChatSessionEvent mutation op: %q", m.Op())
	}
}

// CredentialClient is a client for the Credential schema.
type CredentialClient struct {
	config
}

// NewCredentialClient returns a client for the Credential from the given config.
func NewCredentialClient(c config) *CredentialClient {
	return &CredentialClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `credential.Hooks(f(g(h())))`.
func (c *CredentialClient) Use(hooks ...Hook) {
	c.hooks.Credential = append(c.hooks.Credential, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `credential.Intercept(f(g(h())))`.
func (c *CredentialClient) Intercept(interceptors ...Interceptor) {
	c.inters.Credential = append(c.inters.Credential, interceptors...)
}

// Create returns a builder for creating a Credential entity.
func (c *CredentialClient) Create() *CredentialCreate {
	mutation := newCredentialMutation(c.config, OpCreate)
	return &CredentialCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of Credential entities.
func (c *CredentialClient) CreateBulk(builders ...*CredentialCreate) *CredentialCreateBulk {
	return &CredentialCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *CredentialClient) MapCreateBulk(slice any, setFunc func(*CredentialCreate, int)) *CredentialCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &CredentialCreateBulk{err: fmt.Errorf("calling to CredentialClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*CredentialCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &CredentialCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for Credential.
func (c *CredentialClient) Update() *CredentialUpdate {
	mutation := newCredentialMutation(c.config, OpUpdate)
	return &CredentialUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *CredentialClient) UpdateOne(_m *Credential) *CredentialUpdateOne {
	mutation := newCredentialMutation(c.config, OpUpdateOne, withCredential(_m))
	return &CredentialUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *CredentialClient) UpdateOneID(id uuid.UUID) *CredentialUpdateOne {
	mutation := newCredentialMutation(c.config, OpUpdateOne, withCredentialID(id))
	return &CredentialUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for Credential.
func (c *CredentialClient) Delete() *CredentialDelete {
	mutation := newCredentialMutation(c.config, OpDelete)
	return &CredentialDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *CredentialClient) DeleteOne(_m *Credential) *CredentialDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *CredentialClient) DeleteOneID(id uuid.UUID) *CredentialDeleteOne {
	builder := c.Delete().Where(credential.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &CredentialDeleteOne{builder}
}

// Query returns a query builder for Credential.
func (c *CredentialClient) Query() *CredentialQuery {
	return &CredentialQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeCredential},
		inters: c.Interceptors(),
	}
}

// Get returns a Credential entity by its id.
func (c *CredentialClient) Get(ctx context.Context, id uuid.UUID) (*Credential, error) {
	return c.Query().Where(credential.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *CredentialClient) GetX(ctx context.Context, id uuid.UUID) *Credential {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *CredentialClient) Hooks() []Hook {
	return c.hooks.Credential
}

// Interceptors returns the client interceptors.
func (c *CredentialClient) Interceptors() []Interceptor {
	return c.inters.Credential
}

func (c *CredentialClient) mutate(ctx context.Context, m *CredentialMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&CredentialCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&CredentialUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&CredentialUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&CredentialDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("team: unknown Credential mutation op: %q", m.Op())
	}
}

// DrillReportClient is a client for the DrillReport schema.
type DrillReportClient struct {
	config
}

// NewDrillReportClient returns a client for the DrillReport from the given config.
func NewDrillReportClient(c config) *DrillReportClient {
	return &DrillReportClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `drillreport.Hooks(f(g(h())))`.
func (c *DrillReportClient) Use(hooks ...Hook) {
	c.hooks.DrillReport = append(c.hooks.DrillReport, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `drillreport.Intercept(f(g(h())))`.
func (c *DrillReportClient) Intercept(interceptors ...Interceptor) {
	c.inters.DrillReport = append(c.inters.DrillReport, interceptors...)
}

// Create returns a builder for creating a DrillReport entity.
func (c *DrillReportClient) Create() *DrillReportCreate {
	mutation := newDrillReportMutation(c.config, OpCreate)
	return &DrillReportCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of DrillReport entities.
func (c *DrillReportClient) CreateBulk(builders ...*DrillReportCreate) *DrillReportCreateBulk {
	return &DrillReportCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *DrillReportClient) MapCreateBulk(slice any, setFunc func(*DrillReportCreate, int)) *DrillReportCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &DrillReportCreateBulk{err: fmt.Errorf("calling to DrillReportClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*DrillReportCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &DrillReportCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for DrillReport.
func (c *DrillReportClient) Update() *DrillReportUpdate {
	mutation := newDrillReportMutation(c.config, OpUpdate)
	return &DrillReportUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *DrillReportClient) UpdateOne(_m *DrillReport) *DrillReportUpdateOne {
	mutation := newDrillReportMutation(c.config, OpUpdateOne, withDrillReport(_m))
	return &DrillReportUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *DrillReportClient) UpdateOneID(id uuid.UUID) *DrillReportUpdateOne {
	mutation := newDrillReportMutation(c.config, OpUpdateOne, withDrillReportID(id))
	return &DrillReportUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for DrillReport.
func (c *DrillReportClient) Delete() *DrillReportDelete {
	mutation := newDrillReportMutation(c.config, OpDelete)
	return &DrillReportDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *DrillReportClient) DeleteOne(_m *DrillReport) *DrillReportDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *DrillReportClient) DeleteOneID(id uuid.UUID) *DrillReportDeleteOne {
	builder := c.Delete().Where(drillreport.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &DrillReportDeleteOne{builder}
}

// Query returns a query builder for DrillReport.
func (c *DrillReportClient) Query() *DrillReportQuery {
	return &DrillReportQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeDrillReport},
		inters: c.Interceptors(),
	}
}

// Get returns a DrillReport entity by its id.
func (c *DrillReportClient) Get(ctx context.Context, id uuid.UUID) (*DrillReport, error) {
	return c.Query().Where(drillreport.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *DrillReportClient) GetX(ctx context.Context, id uuid.UUID) *DrillReport {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *DrillReportClient) Hooks() []Hook {
	return c.hooks.DrillReport
}

// Interceptors returns the client interceptors.
func (c *DrillReportClient) Interceptors() []Interceptor {
	return c.inters.DrillReport
}

func (c *DrillReportClient) mutate(ctx context.Context, m *DrillReportMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&DrillReportCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&DrillReportUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&DrillReportUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&DrillReportDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("team: unknown DrillReport mutation op: %q", m.Op())
	}
}

// FleetMailboxMessageClient is a client for the FleetMailboxMessage schema.
type FleetMailboxMessageClient struct {
	config
}

// NewFleetMailboxMessageClient returns a client for the FleetMailboxMessage from the given config.
func NewFleetMailboxMessageClient(c config) *FleetMailboxMessageClient {
	return &FleetMailboxMessageClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `fleetmailboxmessage.Hooks(f(g(h())))`.
func (c *FleetMailboxMessageClient) Use(hooks ...Hook) {
	c.hooks.FleetMailboxMessage = append(c.hooks.FleetMailboxMessage, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `fleetmailboxmessage.Intercept(f(g(h())))`.
func (c *FleetMailboxMessageClient) Intercept(interceptors ...Interceptor) {
	c.inters.FleetMailboxMessage = append(c.inters.FleetMailboxMessage, interceptors...)
}

// Create returns a builder for creating a FleetMailboxMessage entity.
func (c *FleetMailboxMessageClient) Create() *FleetMailboxMessageCreate {
	mutation := newFleetMailboxMessageMutation(c.config, OpCreate)
	return &FleetMailboxMessageCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of FleetMailboxMessage entities.
func (c *FleetMailboxMessageClient) CreateBulk(builders ...*FleetMailboxMessageCreate) *FleetMailboxMessageCreateBulk {
	return &FleetMailboxMessageCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *FleetMailboxMessageClient) MapCreateBulk(slice any, setFunc func(*FleetMailboxMessageCreate, int)) *FleetMailboxMessageCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &FleetMailboxMessageCreateBulk{err: fmt.Errorf("calling to FleetMailboxMessageClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*FleetMailboxMessageCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &FleetMailboxMessageCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for FleetMailboxMessage.
func (c *FleetMailboxMessageClient) Update() *FleetMailboxMessageUpdate {
	mutation := newFleetMailboxMessageMutation(c.config, OpUpdate)
	return &FleetMailboxMessageUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *FleetMailboxMessageClient) UpdateOne(_m *FleetMailboxMessage) *FleetMailboxMessageUpdateOne {
	mutation := newFleetMailboxMessageMutation(c.config, OpUpdateOne, withFleetMailboxMessage(_m))
	return &FleetMailboxMessageUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *FleetMailboxMessageClient) UpdateOneID(id uuid.UUID) *FleetMailboxMessageUpdateOne {
	mutation := newFleetMailboxMessageMutation(c.config, OpUpdateOne, withFleetMailboxMessageID(id))
	return &FleetMailboxMessageUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for FleetMailboxMessage.
func (c *FleetMailboxMessageClient) Delete() *FleetMailboxMessageDelete {
	mutation := newFleetMailboxMessageMutation(c.config, OpDelete)
	return &FleetMailboxMessageDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *FleetMailboxMessageClient) DeleteOne(_m *FleetMailboxMessage) *FleetMailboxMessageDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *FleetMailboxMessageClient) DeleteOneID(id uuid.UUID) *FleetMailboxMessageDeleteOne {
	builder := c.Delete().Where(fleetmailboxmessage.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &FleetMailboxMessageDeleteOne{builder}
}

// Query returns a query builder for FleetMailboxMessage.
func (c *FleetMailboxMessageClient) Query() *FleetMailboxMessageQuery {
	return &FleetMailboxMessageQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeFleetMailboxMessage},
		inters: c.Interceptors(),
	}
}

// Get returns a FleetMailboxMessage entity by its id.
func (c *FleetMailboxMessageClient) Get(ctx context.Context, id uuid.UUID) (*FleetMailboxMessage, error) {
	return c.Query().Where(fleetmailboxmessage.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *FleetMailboxMessageClient) GetX(ctx context.Context, id uuid.UUID) *FleetMailboxMessage {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *FleetMailboxMessageClient) Hooks() []Hook {
	return c.hooks.FleetMailboxMessage
}

// Interceptors returns the client interceptors.
func (c *FleetMailboxMessageClient) Interceptors() []Interceptor {
	return c.inters.FleetMailboxMessage
}

func (c *FleetMailboxMessageClient) mutate(ctx context.Context, m *FleetMailboxMessageMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&FleetMailboxMessageCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&FleetMailboxMessageUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&FleetMailboxMessageUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&FleetMailboxMessageDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("team: unknown FleetMailboxMessage mutation op: %q", m.Op())
	}
}

// FleetMonitorStateClient is a client for the FleetMonitorState schema.
type FleetMonitorStateClient struct {
	config
}

// NewFleetMonitorStateClient returns a client for the FleetMonitorState from the given config.
func NewFleetMonitorStateClient(c config) *FleetMonitorStateClient {
	return &FleetMonitorStateClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `fleetmonitorstate.Hooks(f(g(h())))`.
func (c *FleetMonitorStateClient) Use(hooks ...Hook) {
	c.hooks.FleetMonitorState = append(c.hooks.FleetMonitorState, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `fleetmonitorstate.Intercept(f(g(h())))`.
func (c *FleetMonitorStateClient) Intercept(interceptors ...Interceptor) {
	c.inters.FleetMonitorState = append(c.inters.FleetMonitorState, interceptors...)
}

// Create returns a builder for creating a FleetMonitorState entity.
func (c *FleetMonitorStateClient) Create() *FleetMonitorStateCreate {
	mutation := newFleetMonitorStateMutation(c.config, OpCreate)
	return &FleetMonitorStateCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of FleetMonitorState entities.
func (c *FleetMonitorStateClient) CreateBulk(builders ...*FleetMonitorStateCreate) *FleetMonitorStateCreateBulk {
	return &FleetMonitorStateCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *FleetMonitorStateClient) MapCreateBulk(slice any, setFunc func(*FleetMonitorStateCreate, int)) *FleetMonitorStateCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &FleetMonitorStateCreateBulk{err: fmt.Errorf("calling to FleetMonitorStateClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*FleetMonitorStateCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &FleetMonitorStateCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for FleetMonitorState.
func (c *FleetMonitorStateClient) Update() *FleetMonitorStateUpdate {
	mutation := newFleetMonitorStateMutation(c.config, OpUpdate)
	return &FleetMonitorStateUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *FleetMonitorStateClient) UpdateOne(_m *FleetMonitorState) *FleetMonitorStateUpdateOne {
	mutation := newFleetMonitorStateMutation(c.config, OpUpdateOne, withFleetMonitorState(_m))
	return &FleetMonitorStateUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *FleetMonitorStateClient) UpdateOneID(id string) *FleetMonitorStateUpdateOne {
	mutation := newFleetMonitorStateMutation(c.config, OpUpdateOne, withFleetMonitorStateID(id))
	return &FleetMonitorStateUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for FleetMonitorState.
func (c *FleetMonitorStateClient) Delete() *FleetMonitorStateDelete {
	mutation := newFleetMonitorStateMutation(c.config, OpDelete)
	return &FleetMonitorStateDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *FleetMonitorStateClient) DeleteOne(_m *FleetMonitorState) *FleetMonitorStateDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *FleetMonitorStateClient) DeleteOneID(id string) *FleetMonitorStateDeleteOne {
	builder := c.Delete().Where(fleetmonitorstate.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &FleetMonitorStateDeleteOne{builder}
}

// Query returns a query builder for FleetMonitorState.
func (c *FleetMonitorStateClient) Query() *FleetMonitorStateQuery {
	return &FleetMonitorStateQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeFleetMonitorState},
		inters: c.Interceptors(),
	}
}

// Get returns a FleetMonitorState entity by its id.
func (c *FleetMonitorStateClient) Get(ctx context.Context, id string) (*FleetMonitorState, error) {
	return c.Query().Where(fleetmonitorstate.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *FleetMonitorStateClient) GetX(ctx context.Context, id string) *FleetMonitorState {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *FleetMonitorStateClient) Hooks() []Hook {
	return c.hooks.FleetMonitorState
}

// Interceptors returns the client interceptors.
func (c *FleetMonitorStateClient) Interceptors() []Interceptor {
	return c.inters.FleetMonitorState
}

func (c *FleetMonitorStateClient) mutate(ctx context.Context, m *FleetMonitorStateMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&FleetMonitorStateCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&FleetMonitorStateUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&FleetMonitorStateUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&FleetMonitorStateDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("team: unknown FleetMonitorState mutation op: %q", m.Op())
	}
}

// FleetPlanClient is a client for the FleetPlan schema.
type FleetPlanClient struct {
	config
}

// NewFleetPlanClient returns a client for the FleetPlan from the given config.
func NewFleetPlanClient(c config) *FleetPlanClient {
	return &FleetPlanClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `fleetplan.Hooks(f(g(h())))`.
func (c *FleetPlanClient) Use(hooks ...Hook) {
	c.hooks.FleetPlan = append(c.hooks.FleetPlan, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `fleetplan.Intercept(f(g(h())))`.
func (c *FleetPlanClient) Intercept(interceptors ...Interceptor) {
	c.inters.FleetPlan = append(c.inters.FleetPlan, interceptors...)
}

// Create returns a builder for creating a FleetPlan entity.
func (c *FleetPlanClient) Create() *FleetPlanCreate {
	mutation := newFleetPlanMutation(c.config, OpCreate)
	return &FleetPlanCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of FleetPlan entities.
func (c *FleetPlanClient) CreateBulk(builders ...*FleetPlanCreate) *FleetPlanCreateBulk {
	return &FleetPlanCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *FleetPlanClient) MapCreateBulk(slice any, setFunc func(*FleetPlanCreate, int)) *FleetPlanCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &FleetPlanCreateBulk{err: fmt.Errorf("calling to FleetPlanClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*FleetPlanCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &FleetPlanCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for FleetPlan.
func (c *FleetPlanClient) Update() *FleetPlanUpdate {
	mutation := newFleetPlanMutation(c.config, OpUpdate)
	return &FleetPlanUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *FleetPlanClient) UpdateOne(_m *FleetPlan) *FleetPlanUpdateOne {
	mutation := newFleetPlanMutation(c.config, OpUpdateOne, withFleetPlan(_m))
	return &FleetPlanUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *FleetPlanClient) UpdateOneID(id uuid.UUID) *FleetPlanUpdateOne {
	mutation := newFleetPlanMutation(c.config, OpUpdateOne, withFleetPlanID(id))
	return &FleetPlanUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for FleetPlan.
func (c *FleetPlanClient) Delete() *FleetPlanDelete {
	mutation := newFleetPlanMutation(c.config, OpDelete)
	return &FleetPlanDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *FleetPlanClient) DeleteOne(_m *FleetPlan) *FleetPlanDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *FleetPlanClient) DeleteOneID(id uuid.UUID) *FleetPlanDeleteOne {
	builder := c.Delete().Where(fleetplan.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &FleetPlanDeleteOne{builder}
}

// Query returns a query builder for FleetPlan.
func (c *FleetPlanClient) Query() *FleetPlanQuery {
	return &FleetPlanQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeFleetPlan},
		inters: c.Interceptors(),
	}
}

// Get returns a FleetPlan entity by its id.
func (c *FleetPlanClient) Get(ctx context.Context, id uuid.UUID) (*FleetPlan, error) {
	return c.Query().Where(fleetplan.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *FleetPlanClient) GetX(ctx context.Context, id uuid.UUID) *FleetPlan {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *FleetPlanClient) Hooks() []Hook {
	return c.hooks.FleetPlan
}

// Interceptors returns the client interceptors.
func (c *FleetPlanClient) Interceptors() []Interceptor {
	return c.inters.FleetPlan
}

func (c *FleetPlanClient) mutate(ctx context.Context, m *FleetPlanMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&FleetPlanCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&FleetPlanUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&FleetPlanUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&FleetPlanDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("team: unknown FleetPlan mutation op: %q", m.Op())
	}
}

// FleetRunStateClient is a client for the FleetRunState schema.
type FleetRunStateClient struct {
	config
}

// NewFleetRunStateClient returns a client for the FleetRunState from the given config.
func NewFleetRunStateClient(c config) *FleetRunStateClient {
	return &FleetRunStateClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `fleetrunstate.Hooks(f(g(h())))`.
func (c *FleetRunStateClient) Use(hooks ...Hook) {
	c.hooks.FleetRunState = append(c.hooks.FleetRunState, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `fleetrunstate.Intercept(f(g(h())))`.
func (c *FleetRunStateClient) Intercept(interceptors ...Interceptor) {
	c.inters.FleetRunState = append(c.inters.FleetRunState, interceptors...)
}

// Create returns a builder for creating a FleetRunState entity.
func (c *FleetRunStateClient) Create() *FleetRunStateCreate {
	mutation := newFleetRunStateMutation(c.config, OpCreate)
	return &FleetRunStateCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of FleetRunState entities.
func (c *FleetRunStateClient) CreateBulk(builders ...*FleetRunStateCreate) *FleetRunStateCreateBulk {
	return &FleetRunStateCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *FleetRunStateClient) MapCreateBulk(slice any, setFunc func(*FleetRunStateCreate, int)) *FleetRunStateCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &FleetRunStateCreateBulk{err: fmt.Errorf("calling to FleetRunStateClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*FleetRunStateCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &FleetRunStateCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for FleetRunState.
func (c *FleetRunStateClient) Update() *FleetRunStateUpdate {
	mutation := newFleetRunStateMutation(c.config, OpUpdate)
	return &FleetRunStateUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *FleetRunStateClient) UpdateOne(_m *FleetRunState) *FleetRunStateUpdateOne {
	mutation := newFleetRunStateMutation(c.config, OpUpdateOne, withFleetRunState(_m))
	return &FleetRunStateUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *FleetRunStateClient) UpdateOneID(id uuid.UUID) *FleetRunStateUpdateOne {
	mutation := newFleetRunStateMutation(c.config, OpUpdateOne, withFleetRunStateID(id))
	return &FleetRunStateUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for FleetRunState.
func (c *FleetRunStateClient) Delete() *FleetRunStateDelete {
	mutation := newFleetRunStateMutation(c.config, OpDelete)
	return &FleetRunStateDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *FleetRunStateClient) DeleteOne(_m *FleetRunState) *FleetRunStateDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *FleetRunStateClient) DeleteOneID(id uuid.UUID) *FleetRunStateDeleteOne {
	builder := c.Delete().Where(fleetrunstate.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &FleetRunStateDeleteOne{builder}
}

// Query returns a query builder for FleetRunState.
func (c *FleetRunStateClient) Query() *FleetRunStateQuery {
	return &FleetRunStateQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeFleetRunState},
		inters: c.Interceptors(),
	}
}

// Get returns a FleetRunState entity by its id.
func (c *FleetRunStateClient) Get(ctx context.Context, id uuid.UUID) (*FleetRunState, error) {
	return c.Query().Where(fleetrunstate.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *FleetRunStateClient) GetX(ctx context.Context, id uuid.UUID) *FleetRunState {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *FleetRunStateClient) Hooks() []Hook {
	return c.hooks.FleetRunState
}

// Interceptors returns the client interceptors.
func (c *FleetRunStateClient) Interceptors() []Interceptor {
	return c.inters.FleetRunState
}

func (c *FleetRunStateClient) mutate(ctx context.Context, m *FleetRunStateMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&FleetRunStateCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&FleetRunStateUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&FleetRunStateUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&FleetRunStateDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("team: unknown FleetRunState mutation op: %q", m.Op())
	}
}

// FleetSetupDraftClient is a client for the FleetSetupDraft schema.
type FleetSetupDraftClient struct {
	config
}

// NewFleetSetupDraftClient returns a client for the FleetSetupDraft from the given config.
func NewFleetSetupDraftClient(c config) *FleetSetupDraftClient {
	return &FleetSetupDraftClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `fleetsetupdraft.Hooks(f(g(h())))`.
func (c *FleetSetupDraftClient) Use(hooks ...Hook) {
	c.hooks.FleetSetupDraft = append(c.hooks.FleetSetupDraft, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `fleetsetupdraft.Intercept(f(g(h())))`.
func (c *FleetSetupDraftClient) Intercept(interceptors ...Interceptor) {
	c.inters.FleetSetupDraft = append(c.inters.FleetSetupDraft, interceptors...)
}

// Create returns a builder for creating a FleetSetupDraft entity.
func (c *FleetSetupDraftClient) Create() *FleetSetupDraftCreate {
	mutation := newFleetSetupDraftMutation(c.config, OpCreate)
	return &FleetSetupDraftCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of FleetSetupDraft entities.
func (c *FleetSetupDraftClient) CreateBulk(builders ...*FleetSetupDraftCreate) *FleetSetupDraftCreateBulk {
	return &FleetSetupDraftCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *FleetSetupDraftClient) MapCreateBulk(slice any, setFunc func(*FleetSetupDraftCreate, int)) *FleetSetupDraftCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &FleetSetupDraftCreateBulk{err: fmt.Errorf("calling to FleetSetupDraftClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*FleetSetupDraftCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &FleetSetupDraftCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for FleetSetupDraft.
func (c *FleetSetupDraftClient) Update() *FleetSetupDraftUpdate {
	mutation := newFleetSetupDraftMutation(c.config, OpUpdate)
	return &FleetSetupDraftUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *FleetSetupDraftClient) UpdateOne(_m *FleetSetupDraft) *FleetSetupDraftUpdateOne {
	mutation := newFleetSetupDraftMutation(c.config, OpUpdateOne, withFleetSetupDraft(_m))
	return &FleetSetupDraftUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *FleetSetupDraftClient) UpdateOneID(id uuid.UUID) *FleetSetupDraftUpdateOne {
	mutation := newFleetSetupDraftMutation(c.config, OpUpdateOne, withFleetSetupDraftID(id))
	return &FleetSetupDraftUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for FleetSetupDraft.
func (c *FleetSetupDraftClient) Delete() *FleetSetupDraftDelete {
	mutation := newFleetSetupDraftMutation(c.config, OpDelete)
	return &FleetSetupDraftDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *FleetSetupDraftClient) DeleteOne(_m *FleetSetupDraft) *FleetSetupDraftDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *FleetSetupDraftClient) DeleteOneID(id uuid.UUID) *FleetSetupDraftDeleteOne {
	builder := c.Delete().Where(fleetsetupdraft.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &FleetSetupDraftDeleteOne{builder}
}

// Query returns a query builder for FleetSetupDraft.
func (c *FleetSetupDraftClient) Query() *FleetSetupDraftQuery {
	return &FleetSetupDraftQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeFleetSetupDraft},
		inters: c.Interceptors(),
	}
}

// Get returns a FleetSetupDraft entity by its id.
func (c *FleetSetupDraftClient) Get(ctx context.Context, id uuid.UUID) (*FleetSetupDraft, error) {
	return c.Query().Where(fleetsetupdraft.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *FleetSetupDraftClient) GetX(ctx context.Context, id uuid.UUID) *FleetSetupDraft {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *FleetSetupDraftClient) Hooks() []Hook {
	return c.hooks.FleetSetupDraft
}

// Interceptors returns the client interceptors.
func (c *FleetSetupDraftClient) Interceptors() []Interceptor {
	return c.inters.FleetSetupDraft
}

func (c *FleetSetupDraftClient) mutate(ctx context.Context, m *FleetSetupDraftMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&FleetSetupDraftCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&FleetSetupDraftUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&FleetSetupDraftUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&FleetSetupDraftDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("team: unknown FleetSetupDraft mutation op: %q", m.Op())
	}
}

// FleetSetupProfileClient is a client for the FleetSetupProfile schema.
type FleetSetupProfileClient struct {
	config
}

// NewFleetSetupProfileClient returns a client for the FleetSetupProfile from the given config.
func NewFleetSetupProfileClient(c config) *FleetSetupProfileClient {
	return &FleetSetupProfileClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `fleetsetupprofile.Hooks(f(g(h())))`.
func (c *FleetSetupProfileClient) Use(hooks ...Hook) {
	c.hooks.FleetSetupProfile = append(c.hooks.FleetSetupProfile, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `fleetsetupprofile.Intercept(f(g(h())))`.
func (c *FleetSetupProfileClient) Intercept(interceptors ...Interceptor) {
	c.inters.FleetSetupProfile = append(c.inters.FleetSetupProfile, interceptors...)
}

// Create returns a builder for creating a FleetSetupProfile entity.
func (c *FleetSetupProfileClient) Create() *FleetSetupProfileCreate {
	mutation := newFleetSetupProfileMutation(c.config, OpCreate)
	return &FleetSetupProfileCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of FleetSetupProfile entities.
func (c *FleetSetupProfileClient) CreateBulk(builders ...*FleetSetupProfileCreate) *FleetSetupProfileCreateBulk {
	return &FleetSetupProfileCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *FleetSetupProfileClient) MapCreateBulk(slice any, setFunc func(*FleetSetupProfileCreate, int)) *FleetSetupProfileCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &FleetSetupProfileCreateBulk{err: fmt.Errorf("calling to FleetSetupProfileClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*FleetSetupProfileCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &FleetSetupProfileCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for FleetSetupProfile.
func (c *FleetSetupProfileClient) Update() *FleetSetupProfileUpdate {
	mutation := newFleetSetupProfileMutation(c.config, OpUpdate)
	return &FleetSetupProfileUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *FleetSetupProfileClient) UpdateOne(_m *FleetSetupProfile) *FleetSetupProfileUpdateOne {
	mutation := newFleetSetupProfileMutation(c.config, OpUpdateOne, withFleetSetupProfile(_m))
	return &FleetSetupProfileUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *FleetSetupProfileClient) UpdateOneID(id uuid.UUID) *FleetSetupProfileUpdateOne {
	mutation := newFleetSetupProfileMutation(c.config, OpUpdateOne, withFleetSetupProfileID(id))
	return &FleetSetupProfileUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for FleetSetupProfile.
func (c *FleetSetupProfileClient) Delete() *FleetSetupProfileDelete {
	mutation := newFleetSetupProfileMutation(c.config, OpDelete)
	return &FleetSetupProfileDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *FleetSetupProfileClient) DeleteOne(_m *FleetSetupProfile) *FleetSetupProfileDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *FleetSetupProfileClient) DeleteOneID(id uuid.UUID) *FleetSetupProfileDeleteOne {
	builder := c.Delete().Where(fleetsetupprofile.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &FleetSetupProfileDeleteOne{builder}
}

// Query returns a query builder for FleetSetupProfile.
func (c *FleetSetupProfileClient) Query() *FleetSetupProfileQuery {
	return &FleetSetupProfileQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeFleetSetupProfile},
		inters: c.Interceptors(),
	}
}

// Get returns a FleetSetupProfile entity by its id.
func (c *FleetSetupProfileClient) Get(ctx context.Context, id uuid.UUID) (*FleetSetupProfile, error) {
	return c.Query().Where(fleetsetupprofile.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *FleetSetupProfileClient) GetX(ctx context.Context, id uuid.UUID) *FleetSetupProfile {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *FleetSetupProfileClient) Hooks() []Hook {
	return c.hooks.FleetSetupProfile
}

// Interceptors returns the client interceptors.
func (c *FleetSetupProfileClient) Interceptors() []Interceptor {
	return c.inters.FleetSetupProfile
}

func (c *FleetSetupProfileClient) mutate(ctx context.Context, m *FleetSetupProfileMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&FleetSetupProfileCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&FleetSetupProfileUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&FleetSetupProfileUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&FleetSetupProfileDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("team: unknown FleetSetupProfile mutation op: %q", m.Op())
	}
}

// FleetTaskClient is a client for the FleetTask schema.
type FleetTaskClient struct {
	config
}

// NewFleetTaskClient returns a client for the FleetTask from the given config.
func NewFleetTaskClient(c config) *FleetTaskClient {
	return &FleetTaskClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `fleettask.Hooks(f(g(h())))`.
func (c *FleetTaskClient) Use(hooks ...Hook) {
	c.hooks.FleetTask = append(c.hooks.FleetTask, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `fleettask.Intercept(f(g(h())))`.
func (c *FleetTaskClient) Intercept(interceptors ...Interceptor) {
	c.inters.FleetTask = append(c.inters.FleetTask, interceptors...)
}

// Create returns a builder for creating a FleetTask entity.
func (c *FleetTaskClient) Create() *FleetTaskCreate {
	mutation := newFleetTaskMutation(c.config, OpCreate)
	return &FleetTaskCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of FleetTask entities.
func (c *FleetTaskClient) CreateBulk(builders ...*FleetTaskCreate) *FleetTaskCreateBulk {
	return &FleetTaskCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *FleetTaskClient) MapCreateBulk(slice any, setFunc func(*FleetTaskCreate, int)) *FleetTaskCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &FleetTaskCreateBulk{err: fmt.Errorf("calling to FleetTaskClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*FleetTaskCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &FleetTaskCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for FleetTask.
func (c *FleetTaskClient) Update() *FleetTaskUpdate {
	mutation := newFleetTaskMutation(c.config, OpUpdate)
	return &FleetTaskUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *FleetTaskClient) UpdateOne(_m *FleetTask) *FleetTaskUpdateOne {
	mutation := newFleetTaskMutation(c.config, OpUpdateOne, withFleetTask(_m))
	return &FleetTaskUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *FleetTaskClient) UpdateOneID(id uuid.UUID) *FleetTaskUpdateOne {
	mutation := newFleetTaskMutation(c.config, OpUpdateOne, withFleetTaskID(id))
	return &FleetTaskUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for FleetTask.
func (c *FleetTaskClient) Delete() *FleetTaskDelete {
	mutation := newFleetTaskMutation(c.config, OpDelete)
	return &FleetTaskDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *FleetTaskClient) DeleteOne(_m *FleetTask) *FleetTaskDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *FleetTaskClient) DeleteOneID(id uuid.UUID) *FleetTaskDeleteOne {
	builder := c.Delete().Where(fleettask.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &FleetTaskDeleteOne{builder}
}

// Query returns a query builder for FleetTask.
func (c *FleetTaskClient) Query() *FleetTaskQuery {
	return &FleetTaskQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeFleetTask},
		inters: c.Interceptors(),
	}
}

// Get returns a FleetTask entity by its id.
func (c *FleetTaskClient) Get(ctx context.Context, id uuid.UUID) (*FleetTask, error) {
	return c.Query().Where(fleettask.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *FleetTaskClient) GetX(ctx context.Context, id uuid.UUID) *FleetTask {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *FleetTaskClient) Hooks() []Hook {
	return c.hooks.FleetTask
}

// Interceptors returns the client interceptors.
func (c *FleetTaskClient) Interceptors() []Interceptor {
	return c.inters.FleetTask
}

func (c *FleetTaskClient) mutate(ctx context.Context, m *FleetTaskMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&FleetTaskCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&FleetTaskUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&FleetTaskUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&FleetTaskDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("team: unknown FleetTask mutation op: %q", m.Op())
	}
}

// FleetTemplateClient is a client for the FleetTemplate schema.
type FleetTemplateClient struct {
	config
}

// NewFleetTemplateClient returns a client for the FleetTemplate from the given config.
func NewFleetTemplateClient(c config) *FleetTemplateClient {
	return &FleetTemplateClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `fleettemplate.Hooks(f(g(h())))`.
func (c *FleetTemplateClient) Use(hooks ...Hook) {
	c.hooks.FleetTemplate = append(c.hooks.FleetTemplate, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `fleettemplate.Intercept(f(g(h())))`.
func (c *FleetTemplateClient) Intercept(interceptors ...Interceptor) {
	c.inters.FleetTemplate = append(c.inters.FleetTemplate, interceptors...)
}

// Create returns a builder for creating a FleetTemplate entity.
func (c *FleetTemplateClient) Create() *FleetTemplateCreate {
	mutation := newFleetTemplateMutation(c.config, OpCreate)
	return &FleetTemplateCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of FleetTemplate entities.
func (c *FleetTemplateClient) CreateBulk(builders ...*FleetTemplateCreate) *FleetTemplateCreateBulk {
	return &FleetTemplateCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *FleetTemplateClient) MapCreateBulk(slice any, setFunc func(*FleetTemplateCreate, int)) *FleetTemplateCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &FleetTemplateCreateBulk{err: fmt.Errorf("calling to FleetTemplateClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*FleetTemplateCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &FleetTemplateCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for FleetTemplate.
func (c *FleetTemplateClient) Update() *FleetTemplateUpdate {
	mutation := newFleetTemplateMutation(c.config, OpUpdate)
	return &FleetTemplateUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *FleetTemplateClient) UpdateOne(_m *FleetTemplate) *FleetTemplateUpdateOne {
	mutation := newFleetTemplateMutation(c.config, OpUpdateOne, withFleetTemplate(_m))
	return &FleetTemplateUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *FleetTemplateClient) UpdateOneID(id uuid.UUID) *FleetTemplateUpdateOne {
	mutation := newFleetTemplateMutation(c.config, OpUpdateOne, withFleetTemplateID(id))
	return &FleetTemplateUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for FleetTemplate.
func (c *FleetTemplateClient) Delete() *FleetTemplateDelete {
	mutation := newFleetTemplateMutation(c.config, OpDelete)
	return &FleetTemplateDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *FleetTemplateClient) DeleteOne(_m *FleetTemplate) *FleetTemplateDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *FleetTemplateClient) DeleteOneID(id uuid.UUID) *FleetTemplateDeleteOne {
	builder := c.Delete().Where(fleettemplate.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &FleetTemplateDeleteOne{builder}
}

// Query returns a query builder for FleetTemplate.
func (c *FleetTemplateClient) Query() *FleetTemplateQuery {
	return &FleetTemplateQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeFleetTemplate},
		inters: c.Interceptors(),
	}
}

// Get returns a FleetTemplate entity by its id.
func (c *FleetTemplateClient) Get(ctx context.Context, id uuid.UUID) (*FleetTemplate, error) {
	return c.Query().Where(fleettemplate.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *FleetTemplateClient) GetX(ctx context.Context, id uuid.UUID) *FleetTemplate {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *FleetTemplateClient) Hooks() []Hook {
	return c.hooks.FleetTemplate
}

// Interceptors returns the client interceptors.
func (c *FleetTemplateClient) Interceptors() []Interceptor {
	return c.inters.FleetTemplate
}

func (c *FleetTemplateClient) mutate(ctx context.Context, m *FleetTemplateMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&FleetTemplateCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&FleetTemplateUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&FleetTemplateUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&FleetTemplateDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("team: unknown FleetTemplate mutation op: %q", m.Op())
	}
}

// FlowClient is a client for the Flow schema.
type FlowClient struct {
	config
}

// NewFlowClient returns a client for the Flow from the given config.
func NewFlowClient(c config) *FlowClient {
	return &FlowClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `flow.Hooks(f(g(h())))`.
func (c *FlowClient) Use(hooks ...Hook) {
	c.hooks.Flow = append(c.hooks.Flow, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `flow.Intercept(f(g(h())))`.
func (c *FlowClient) Intercept(interceptors ...Interceptor) {
	c.inters.Flow = append(c.inters.Flow, interceptors...)
}

// Create returns a builder for creating a Flow entity.
func (c *FlowClient) Create() *FlowCreate {
	mutation := newFlowMutation(c.config, OpCreate)
	return &FlowCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of Flow entities.
func (c *FlowClient) CreateBulk(builders ...*FlowCreate) *FlowCreateBulk {
	return &FlowCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *FlowClient) MapCreateBulk(slice any, setFunc func(*FlowCreate, int)) *FlowCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &FlowCreateBulk{err: fmt.Errorf("calling to FlowClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*FlowCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &FlowCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for Flow.
func (c *FlowClient) Update() *FlowUpdate {
	mutation := newFlowMutation(c.config, OpUpdate)
	return &FlowUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *FlowClient) UpdateOne(_m *Flow) *FlowUpdateOne {
	mutation := newFlowMutation(c.config, OpUpdateOne, withFlow(_m))
	return &FlowUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *FlowClient) UpdateOneID(id uuid.UUID) *FlowUpdateOne {
	mutation := newFlowMutation(c.config, OpUpdateOne, withFlowID(id))
	return &FlowUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for Flow.
func (c *FlowClient) Delete() *FlowDelete {
	mutation := newFlowMutation(c.config, OpDelete)
	return &FlowDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *FlowClient) DeleteOne(_m *Flow) *FlowDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *FlowClient) DeleteOneID(id uuid.UUID) *FlowDeleteOne {
	builder := c.Delete().Where(flow.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &FlowDeleteOne{builder}
}

// Query returns a query builder for Flow.
func (c *FlowClient) Query() *FlowQuery {
	return &FlowQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeFlow},
		inters: c.Interceptors(),
	}
}

// Get returns a Flow entity by its id.
func (c *FlowClient) Get(ctx context.Context, id uuid.UUID) (*Flow, error) {
	return c.Query().Where(flow.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *FlowClient) GetX(ctx context.Context, id uuid.UUID) *Flow {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *FlowClient) Hooks() []Hook {
	return c.hooks.Flow
}

// Interceptors returns the client interceptors.
func (c *FlowClient) Interceptors() []Interceptor {
	return c.inters.Flow
}

func (c *FlowClient) mutate(ctx context.Context, m *FlowMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&FlowCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&FlowUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&FlowUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&FlowDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("team: unknown Flow mutation op: %q", m.Op())
	}
}

// McpServerClient is a client for the McpServer schema.
type McpServerClient struct {
	config
}

// NewMcpServerClient returns a client for the McpServer from the given config.
func NewMcpServerClient(c config) *McpServerClient {
	return &McpServerClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `mcpserver.Hooks(f(g(h())))`.
func (c *McpServerClient) Use(hooks ...Hook) {
	c.hooks.McpServer = append(c.hooks.McpServer, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `mcpserver.Intercept(f(g(h())))`.
func (c *McpServerClient) Intercept(interceptors ...Interceptor) {
	c.inters.McpServer = append(c.inters.McpServer, interceptors...)
}

// Create returns a builder for creating a McpServer entity.
func (c *McpServerClient) Create() *McpServerCreate {
	mutation := newMcpServerMutation(c.config, OpCreate)
	return &McpServerCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of McpServer entities.
func (c *McpServerClient) CreateBulk(builders ...*McpServerCreate) *McpServerCreateBulk {
	return &McpServerCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *McpServerClient) MapCreateBulk(slice any, setFunc func(*McpServerCreate, int)) *McpServerCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &McpServerCreateBulk{err: fmt.Errorf("calling to McpServerClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*McpServerCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &McpServerCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for McpServer.
func (c *McpServerClient) Update() *McpServerUpdate {
	mutation := newMcpServerMutation(c.config, OpUpdate)
	return &McpServerUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *McpServerClient) UpdateOne(_m *McpServer) *McpServerUpdateOne {
	mutation := newMcpServerMutation(c.config, OpUpdateOne, withMcpServer(_m))
	return &McpServerUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *McpServerClient) UpdateOneID(id uuid.UUID) *McpServerUpdateOne {
	mutation := newMcpServerMutation(c.config, OpUpdateOne, withMcpServerID(id))
	return &McpServerUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for McpServer.
func (c *McpServerClient) Delete() *McpServerDelete {
	mutation := newMcpServerMutation(c.config, OpDelete)
	return &McpServerDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *McpServerClient) DeleteOne(_m *McpServer) *McpServerDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *McpServerClient) DeleteOneID(id uuid.UUID) *McpServerDeleteOne {
	builder := c.Delete().Where(mcpserver.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &McpServerDeleteOne{builder}
}

// Query returns a query builder for McpServer.
func (c *McpServerClient) Query() *McpServerQuery {
	return &McpServerQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeMcpServer},
		inters: c.Interceptors(),
	}
}

// Get returns a McpServer entity by its id.
func (c *McpServerClient) Get(ctx context.Context, id uuid.UUID) (*McpServer, error) {
	return c.Query().Where(mcpserver.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *McpServerClient) GetX(ctx context.Context, id uuid.UUID) *McpServer {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *McpServerClient) Hooks() []Hook {
	return c.hooks.McpServer
}

// Interceptors returns the client interceptors.
func (c *McpServerClient) Interceptors() []Interceptor {
	return c.inters.McpServer
}

func (c *McpServerClient) mutate(ctx context.Context, m *McpServerMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&McpServerCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&McpServerUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&McpServerUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&McpServerDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("team: unknown McpServer mutation op: %q", m.Op())
	}
}

// MemoryClient is a client for the Memory schema.
type MemoryClient struct {
	config
}

// NewMemoryClient returns a client for the Memory from the given config.
func NewMemoryClient(c config) *MemoryClient {
	return &MemoryClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `memory.Hooks(f(g(h())))`.
func (c *MemoryClient) Use(hooks ...Hook) {
	c.hooks.Memory = append(c.hooks.Memory, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `memory.Intercept(f(g(h())))`.
func (c *MemoryClient) Intercept(interceptors ...Interceptor) {
	c.inters.Memory = append(c.inters.Memory, interceptors...)
}

// Create returns a builder for creating a Memory entity.
func (c *MemoryClient) Create() *MemoryCreate {
	mutation := newMemoryMutation(c.config, OpCreate)
	return &MemoryCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of Memory entities.
func (c *MemoryClient) CreateBulk(builders ...*MemoryCreate) *MemoryCreateBulk {
	return &MemoryCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *MemoryClient) MapCreateBulk(slice any, setFunc func(*MemoryCreate, int)) *MemoryCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &MemoryCreateBulk{err: fmt.Errorf("calling to MemoryClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*MemoryCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &MemoryCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for Memory.
func (c *MemoryClient) Update() *MemoryUpdate {
	mutation := newMemoryMutation(c.config, OpUpdate)
	return &MemoryUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *MemoryClient) UpdateOne(_m *Memory) *MemoryUpdateOne {
	mutation := newMemoryMutation(c.config, OpUpdateOne, withMemory(_m))
	return &MemoryUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *MemoryClient) UpdateOneID(id uuid.UUID) *MemoryUpdateOne {
	mutation := newMemoryMutation(c.config, OpUpdateOne, withMemoryID(id))
	return &MemoryUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for Memory.
func (c *MemoryClient) Delete() *MemoryDelete {
	mutation := newMemoryMutation(c.config, OpDelete)
	return &MemoryDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *MemoryClient) DeleteOne(_m *Memory) *MemoryDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *MemoryClient) DeleteOneID(id uuid.UUID) *MemoryDeleteOne {
	builder := c.Delete().Where(memory.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &MemoryDeleteOne{builder}
}

// Query returns a query builder for Memory.
func (c *MemoryClient) Query() *MemoryQuery {
	return &MemoryQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeMemory},
		inters: c.Interceptors(),
	}
}

// Get returns a Memory entity by its id.
func (c *MemoryClient) Get(ctx context.Context, id uuid.UUID) (*Memory, error) {
	return c.Query().Where(memory.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *MemoryClient) GetX(ctx context.Context, id uuid.UUID) *Memory {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *MemoryClient) Hooks() []Hook {
	return c.hooks.Memory
}

// Interceptors returns the client interceptors.
func (c *MemoryClient) Interceptors() []Interceptor {
	return c.inters.Memory
}

func (c *MemoryClient) mutate(ctx context.Context, m *MemoryMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&MemoryCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&MemoryUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&MemoryUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&MemoryDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("team: unknown Memory mutation op: %q", m.Op())
	}
}

// NetworkPolicyClient is a client for the NetworkPolicy schema.
type NetworkPolicyClient struct {
	config
}

// NewNetworkPolicyClient returns a client for the NetworkPolicy from the given config.
func NewNetworkPolicyClient(c config) *NetworkPolicyClient {
	return &NetworkPolicyClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `networkpolicy.Hooks(f(g(h())))`.
func (c *NetworkPolicyClient) Use(hooks ...Hook) {
	c.hooks.NetworkPolicy = append(c.hooks.NetworkPolicy, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `networkpolicy.Intercept(f(g(h())))`.
func (c *NetworkPolicyClient) Intercept(interceptors ...Interceptor) {
	c.inters.NetworkPolicy = append(c.inters.NetworkPolicy, interceptors...)
}

// Create returns a builder for creating a NetworkPolicy entity.
func (c *NetworkPolicyClient) Create() *NetworkPolicyCreate {
	mutation := newNetworkPolicyMutation(c.config, OpCreate)
	return &NetworkPolicyCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of NetworkPolicy entities.
func (c *NetworkPolicyClient) CreateBulk(builders ...*NetworkPolicyCreate) *NetworkPolicyCreateBulk {
	return &NetworkPolicyCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *NetworkPolicyClient) MapCreateBulk(slice any, setFunc func(*NetworkPolicyCreate, int)) *NetworkPolicyCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &NetworkPolicyCreateBulk{err: fmt.Errorf("calling to NetworkPolicyClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*NetworkPolicyCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &NetworkPolicyCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for NetworkPolicy.
func (c *NetworkPolicyClient) Update() *NetworkPolicyUpdate {
	mutation := newNetworkPolicyMutation(c.config, OpUpdate)
	return &NetworkPolicyUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *NetworkPolicyClient) UpdateOne(_m *NetworkPolicy) *NetworkPolicyUpdateOne {
	mutation := newNetworkPolicyMutation(c.config, OpUpdateOne, withNetworkPolicy(_m))
	return &NetworkPolicyUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *NetworkPolicyClient) UpdateOneID(id uuid.UUID) *NetworkPolicyUpdateOne {
	mutation := newNetworkPolicyMutation(c.config, OpUpdateOne, withNetworkPolicyID(id))
	return &NetworkPolicyUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for NetworkPolicy.
func (c *NetworkPolicyClient) Delete() *NetworkPolicyDelete {
	mutation := newNetworkPolicyMutation(c.config, OpDelete)
	return &NetworkPolicyDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *NetworkPolicyClient) DeleteOne(_m *NetworkPolicy) *NetworkPolicyDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *NetworkPolicyClient) DeleteOneID(id uuid.UUID) *NetworkPolicyDeleteOne {
	builder := c.Delete().Where(networkpolicy.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &NetworkPolicyDeleteOne{builder}
}

// Query returns a query builder for NetworkPolicy.
func (c *NetworkPolicyClient) Query() *NetworkPolicyQuery {
	return &NetworkPolicyQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeNetworkPolicy},
		inters: c.Interceptors(),
	}
}

// Get returns a NetworkPolicy entity by its id.
func (c *NetworkPolicyClient) Get(ctx context.Context, id uuid.UUID) (*NetworkPolicy, error) {
	return c.Query().Where(networkpolicy.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *NetworkPolicyClient) GetX(ctx context.Context, id uuid.UUID) *NetworkPolicy {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *NetworkPolicyClient) Hooks() []Hook {
	return c.hooks.NetworkPolicy
}

// Interceptors returns the client interceptors.
func (c *NetworkPolicyClient) Interceptors() []Interceptor {
	return c.inters.NetworkPolicy
}

func (c *NetworkPolicyClient) mutate(ctx context.Context, m *NetworkPolicyMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&NetworkPolicyCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&NetworkPolicyUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&NetworkPolicyUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&NetworkPolicyDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("team: unknown NetworkPolicy mutation op: %q", m.Op())
	}
}

// SandboxSessionClient is a client for the SandboxSession schema.
type SandboxSessionClient struct {
	config
}

// NewSandboxSessionClient returns a client for the SandboxSession from the given config.
func NewSandboxSessionClient(c config) *SandboxSessionClient {
	return &SandboxSessionClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `sandboxsession.Hooks(f(g(h())))`.
func (c *SandboxSessionClient) Use(hooks ...Hook) {
	c.hooks.SandboxSession = append(c.hooks.SandboxSession, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `sandboxsession.Intercept(f(g(h())))`.
func (c *SandboxSessionClient) Intercept(interceptors ...Interceptor) {
	c.inters.SandboxSession = append(c.inters.SandboxSession, interceptors...)
}

// Create returns a builder for creating a SandboxSession entity.
func (c *SandboxSessionClient) Create() *SandboxSessionCreate {
	mutation := newSandboxSessionMutation(c.config, OpCreate)
	return &SandboxSessionCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of SandboxSession entities.
func (c *SandboxSessionClient) CreateBulk(builders ...*SandboxSessionCreate) *SandboxSessionCreateBulk {
	return &SandboxSessionCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *SandboxSessionClient) MapCreateBulk(slice any, setFunc func(*SandboxSessionCreate, int)) *SandboxSessionCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &SandboxSessionCreateBulk{err: fmt.Errorf("calling to SandboxSessionClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*SandboxSessionCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &SandboxSessionCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for SandboxSession.
func (c *SandboxSessionClient) Update() *SandboxSessionUpdate {
	mutation := newSandboxSessionMutation(c.config, OpUpdate)
	return &SandboxSessionUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *SandboxSessionClient) UpdateOne(_m *SandboxSession) *SandboxSessionUpdateOne {
	mutation := newSandboxSessionMutation(c.config, OpUpdateOne, withSandboxSession(_m))
	return &SandboxSessionUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *SandboxSessionClient) UpdateOneID(id string) *SandboxSessionUpdateOne {
	mutation := newSandboxSessionMutation(c.config, OpUpdateOne, withSandboxSessionID(id))
	return &SandboxSessionUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for SandboxSession.
func (c *SandboxSessionClient) Delete() *SandboxSessionDelete {
	mutation := newSandboxSessionMutation(c.config, OpDelete)
	return &SandboxSessionDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *SandboxSessionClient) DeleteOne(_m *SandboxSession) *SandboxSessionDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *SandboxSessionClient) DeleteOneID(id string) *SandboxSessionDeleteOne {
	builder := c.Delete().Where(sandboxsession.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &SandboxSessionDeleteOne{builder}
}

// Query returns a query builder for SandboxSession.
func (c *SandboxSessionClient) Query() *SandboxSessionQuery {
	return &SandboxSessionQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeSandboxSession},
		inters: c.Interceptors(),
	}
}

// Get returns a SandboxSession entity by its id.
func (c *SandboxSessionClient) Get(ctx context.Context, id string) (*SandboxSession, error) {
	return c.Query().Where(sandboxsession.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *SandboxSessionClient) GetX(ctx context.Context, id string) *SandboxSession {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *SandboxSessionClient) Hooks() []Hook {
	return c.hooks.SandboxSession
}

// Interceptors returns the client interceptors.
func (c *SandboxSessionClient) Interceptors() []Interceptor {
	return c.inters.SandboxSession
}

func (c *SandboxSessionClient) mutate(ctx context.Context, m *SandboxSessionMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&SandboxSessionCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&SandboxSessionUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&SandboxSessionUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&SandboxSessionDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("team: unknown SandboxSession mutation op: %q", m.Op())
	}
}

// ScheduledJobClient is a client for the ScheduledJob schema.
type ScheduledJobClient struct {
	config
}

// NewScheduledJobClient returns a client for the ScheduledJob from the given config.
func NewScheduledJobClient(c config) *ScheduledJobClient {
	return &ScheduledJobClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `scheduledjob.Hooks(f(g(h())))`.
func (c *ScheduledJobClient) Use(hooks ...Hook) {
	c.hooks.ScheduledJob = append(c.hooks.ScheduledJob, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `scheduledjob.Intercept(f(g(h())))`.
func (c *ScheduledJobClient) Intercept(interceptors ...Interceptor) {
	c.inters.ScheduledJob = append(c.inters.ScheduledJob, interceptors...)
}

// Create returns a builder for creating a ScheduledJob entity.
func (c *ScheduledJobClient) Create() *ScheduledJobCreate {
	mutation := newScheduledJobMutation(c.config, OpCreate)
	return &ScheduledJobCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of ScheduledJob entities.
func (c *ScheduledJobClient) CreateBulk(builders ...*ScheduledJobCreate) *ScheduledJobCreateBulk {
	return &ScheduledJobCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *ScheduledJobClient) MapCreateBulk(slice any, setFunc func(*ScheduledJobCreate, int)) *ScheduledJobCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &ScheduledJobCreateBulk{err: fmt.Errorf("calling to ScheduledJobClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*ScheduledJobCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &ScheduledJobCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for ScheduledJob.
func (c *ScheduledJobClient) Update() *ScheduledJobUpdate {
	mutation := newScheduledJobMutation(c.config, OpUpdate)
	return &ScheduledJobUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *ScheduledJobClient) UpdateOne(_m *ScheduledJob) *ScheduledJobUpdateOne {
	mutation := newScheduledJobMutation(c.config, OpUpdateOne, withScheduledJob(_m))
	return &ScheduledJobUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *ScheduledJobClient) UpdateOneID(id uuid.UUID) *ScheduledJobUpdateOne {
	mutation := newScheduledJobMutation(c.config, OpUpdateOne, withScheduledJobID(id))
	return &ScheduledJobUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for ScheduledJob.
func (c *ScheduledJobClient) Delete() *ScheduledJobDelete {
	mutation := newScheduledJobMutation(c.config, OpDelete)
	return &ScheduledJobDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *ScheduledJobClient) DeleteOne(_m *ScheduledJob) *ScheduledJobDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *ScheduledJobClient) DeleteOneID(id uuid.UUID) *ScheduledJobDeleteOne {
	builder := c.Delete().Where(scheduledjob.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &ScheduledJobDeleteOne{builder}
}

// Query returns a query builder for ScheduledJob.
func (c *ScheduledJobClient) Query() *ScheduledJobQuery {
	return &ScheduledJobQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeScheduledJob},
		inters: c.Interceptors(),
	}
}

// Get returns a ScheduledJob entity by its id.
func (c *ScheduledJobClient) Get(ctx context.Context, id uuid.UUID) (*ScheduledJob, error) {
	return c.Query().Where(scheduledjob.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *ScheduledJobClient) GetX(ctx context.Context, id uuid.UUID) *ScheduledJob {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *ScheduledJobClient) Hooks() []Hook {
	return c.hooks.ScheduledJob
}

// Interceptors returns the client interceptors.
func (c *ScheduledJobClient) Interceptors() []Interceptor {
	return c.inters.ScheduledJob
}

func (c *ScheduledJobClient) mutate(ctx context.Context, m *ScheduledJobMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&ScheduledJobCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&ScheduledJobUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&ScheduledJobUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&ScheduledJobDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("team: unknown ScheduledJob mutation op: %q", m.Op())
	}
}

// SessionClient is a client for the Session schema.
type SessionClient struct {
	config
}

// NewSessionClient returns a client for the Session from the given config.
func NewSessionClient(c config) *SessionClient {
	return &SessionClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `session.Hooks(f(g(h())))`.
func (c *SessionClient) Use(hooks ...Hook) {
	c.hooks.Session = append(c.hooks.Session, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `session.Intercept(f(g(h())))`.
func (c *SessionClient) Intercept(interceptors ...Interceptor) {
	c.inters.Session = append(c.inters.Session, interceptors...)
}

// Create returns a builder for creating a Session entity.
func (c *SessionClient) Create() *SessionCreate {
	mutation := newSessionMutation(c.config, OpCreate)
	return &SessionCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of Session entities.
func (c *SessionClient) CreateBulk(builders ...*SessionCreate) *SessionCreateBulk {
	return &SessionCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *SessionClient) MapCreateBulk(slice any, setFunc func(*SessionCreate, int)) *SessionCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &SessionCreateBulk{err: fmt.Errorf("calling to SessionClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*SessionCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &SessionCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for Session.
func (c *SessionClient) Update() *SessionUpdate {
	mutation := newSessionMutation(c.config, OpUpdate)
	return &SessionUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *SessionClient) UpdateOne(_m *Session) *SessionUpdateOne {
	mutation := newSessionMutation(c.config, OpUpdateOne, withSession(_m))
	return &SessionUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *SessionClient) UpdateOneID(id string) *SessionUpdateOne {
	mutation := newSessionMutation(c.config, OpUpdateOne, withSessionID(id))
	return &SessionUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for Session.
func (c *SessionClient) Delete() *SessionDelete {
	mutation := newSessionMutation(c.config, OpDelete)
	return &SessionDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *SessionClient) DeleteOne(_m *Session) *SessionDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *SessionClient) DeleteOneID(id string) *SessionDeleteOne {
	builder := c.Delete().Where(session.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &SessionDeleteOne{builder}
}

// Query returns a query builder for Session.
func (c *SessionClient) Query() *SessionQuery {
	return &SessionQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeSession},
		inters: c.Interceptors(),
	}
}

// Get returns a Session entity by its id.
func (c *SessionClient) Get(ctx context.Context, id string) (*Session, error) {
	return c.Query().Where(session.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *SessionClient) GetX(ctx context.Context, id string) *Session {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// QueryEvents queries the events edge of a Session.
func (c *SessionClient) QueryEvents(_m *Session) *SessionEventQuery {
	query := (&SessionEventClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := _m.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(session.Table, session.FieldID, id),
			sqlgraph.To(sessionevent.Table, sessionevent.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, session.EventsTable, session.EventsColumn),
		)
		fromV = sqlgraph.Neighbors(_m.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QueryChatEvents queries the chat_events edge of a Session.
func (c *SessionClient) QueryChatEvents(_m *Session) *ChatSessionEventQuery {
	query := (&ChatSessionEventClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := _m.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(session.Table, session.FieldID, id),
			sqlgraph.To(chatsessionevent.Table, chatsessionevent.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, session.ChatEventsTable, session.ChatEventsColumn),
		)
		fromV = sqlgraph.Neighbors(_m.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// Hooks returns the client hooks.
func (c *SessionClient) Hooks() []Hook {
	return c.hooks.Session
}

// Interceptors returns the client interceptors.
func (c *SessionClient) Interceptors() []Interceptor {
	return c.inters.Session
}

func (c *SessionClient) mutate(ctx context.Context, m *SessionMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&SessionCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&SessionUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&SessionUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&SessionDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("team: unknown Session mutation op: %q", m.Op())
	}
}

// SessionEventClient is a client for the SessionEvent schema.
type SessionEventClient struct {
	config
}

// NewSessionEventClient returns a client for the SessionEvent from the given config.
func NewSessionEventClient(c config) *SessionEventClient {
	return &SessionEventClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `sessionevent.Hooks(f(g(h())))`.
func (c *SessionEventClient) Use(hooks ...Hook) {
	c.hooks.SessionEvent = append(c.hooks.SessionEvent, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `sessionevent.Intercept(f(g(h())))`.
func (c *SessionEventClient) Intercept(interceptors ...Interceptor) {
	c.inters.SessionEvent = append(c.inters.SessionEvent, interceptors...)
}

// Create returns a builder for creating a SessionEvent entity.
func (c *SessionEventClient) Create() *SessionEventCreate {
	mutation := newSessionEventMutation(c.config, OpCreate)
	return &SessionEventCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of SessionEvent entities.
func (c *SessionEventClient) CreateBulk(builders ...*SessionEventCreate) *SessionEventCreateBulk {
	return &SessionEventCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *SessionEventClient) MapCreateBulk(slice any, setFunc func(*SessionEventCreate, int)) *SessionEventCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &SessionEventCreateBulk{err: fmt.Errorf("calling to SessionEventClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*SessionEventCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &SessionEventCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for SessionEvent.
func (c *SessionEventClient) Update() *SessionEventUpdate {
	mutation := newSessionEventMutation(c.config, OpUpdate)
	return &SessionEventUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *SessionEventClient) UpdateOne(_m *SessionEvent) *SessionEventUpdateOne {
	mutation := newSessionEventMutation(c.config, OpUpdateOne, withSessionEvent(_m))
	return &SessionEventUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *SessionEventClient) UpdateOneID(id int64) *SessionEventUpdateOne {
	mutation := newSessionEventMutation(c.config, OpUpdateOne, withSessionEventID(id))
	return &SessionEventUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for SessionEvent.
func (c *SessionEventClient) Delete() *SessionEventDelete {
	mutation := newSessionEventMutation(c.config, OpDelete)
	return &SessionEventDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *SessionEventClient) DeleteOne(_m *SessionEvent) *SessionEventDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *SessionEventClient) DeleteOneID(id int64) *SessionEventDeleteOne {
	builder := c.Delete().Where(sessionevent.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &SessionEventDeleteOne{builder}
}

// Query returns a query builder for SessionEvent.
func (c *SessionEventClient) Query() *SessionEventQuery {
	return &SessionEventQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeSessionEvent},
		inters: c.Interceptors(),
	}
}

// Get returns a SessionEvent entity by its id.
func (c *SessionEventClient) Get(ctx context.Context, id int64) (*SessionEvent, error) {
	return c.Query().Where(sessionevent.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *SessionEventClient) GetX(ctx context.Context, id int64) *SessionEvent {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// QuerySession queries the session edge of a SessionEvent.
func (c *SessionEventClient) QuerySession(_m *SessionEvent) *SessionQuery {
	query := (&SessionClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := _m.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(sessionevent.Table, sessionevent.FieldID, id),
			sqlgraph.To(session.Table, session.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, sessionevent.SessionTable, sessionevent.SessionColumn),
		)
		fromV = sqlgraph.Neighbors(_m.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// Hooks returns the client hooks.
func (c *SessionEventClient) Hooks() []Hook {
	return c.hooks.SessionEvent
}

// Interceptors returns the client interceptors.
func (c *SessionEventClient) Interceptors() []Interceptor {
	return c.inters.SessionEvent
}

func (c *SessionEventClient) mutate(ctx context.Context, m *SessionEventMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&SessionEventCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&SessionEventUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&SessionEventUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&SessionEventDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("team: unknown SessionEvent mutation op: %q", m.Op())
	}
}

// SettingClient is a client for the Setting schema.
type SettingClient struct {
	config
}

// NewSettingClient returns a client for the Setting from the given config.
func NewSettingClient(c config) *SettingClient {
	return &SettingClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `setting.Hooks(f(g(h())))`.
func (c *SettingClient) Use(hooks ...Hook) {
	c.hooks.Setting = append(c.hooks.Setting, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `setting.Intercept(f(g(h())))`.
func (c *SettingClient) Intercept(interceptors ...Interceptor) {
	c.inters.Setting = append(c.inters.Setting, interceptors...)
}

// Create returns a builder for creating a Setting entity.
func (c *SettingClient) Create() *SettingCreate {
	mutation := newSettingMutation(c.config, OpCreate)
	return &SettingCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of Setting entities.
func (c *SettingClient) CreateBulk(builders ...*SettingCreate) *SettingCreateBulk {
	return &SettingCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *SettingClient) MapCreateBulk(slice any, setFunc func(*SettingCreate, int)) *SettingCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &SettingCreateBulk{err: fmt.Errorf("calling to SettingClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*SettingCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &SettingCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for Setting.
func (c *SettingClient) Update() *SettingUpdate {
	mutation := newSettingMutation(c.config, OpUpdate)
	return &SettingUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *SettingClient) UpdateOne(_m *Setting) *SettingUpdateOne {
	mutation := newSettingMutation(c.config, OpUpdateOne, withSetting(_m))
	return &SettingUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *SettingClient) UpdateOneID(id string) *SettingUpdateOne {
	mutation := newSettingMutation(c.config, OpUpdateOne, withSettingID(id))
	return &SettingUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for Setting.
func (c *SettingClient) Delete() *SettingDelete {
	mutation := newSettingMutation(c.config, OpDelete)
	return &SettingDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *SettingClient) DeleteOne(_m *Setting) *SettingDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *SettingClient) DeleteOneID(id string) *SettingDeleteOne {
	builder := c.Delete().Where(setting.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &SettingDeleteOne{builder}
}

// Query returns a query builder for Setting.
func (c *SettingClient) Query() *SettingQuery {
	return &SettingQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeSetting},
		inters: c.Interceptors(),
	}
}

// Get returns a Setting entity by its id.
func (c *SettingClient) Get(ctx context.Context, id string) (*Setting, error) {
	return c.Query().Where(setting.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *SettingClient) GetX(ctx context.Context, id string) *Setting {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *SettingClient) Hooks() []Hook {
	return c.hooks.Setting
}

// Interceptors returns the client interceptors.
func (c *SettingClient) Interceptors() []Interceptor {
	return c.inters.Setting
}

func (c *SettingClient) mutate(ctx context.Context, m *SettingMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&SettingCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&SettingUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&SettingUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&SettingDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("team: unknown Setting mutation op: %q", m.Op())
	}
}

// SkillClient is a client for the Skill schema.
type SkillClient struct {
	config
}

// NewSkillClient returns a client for the Skill from the given config.
func NewSkillClient(c config) *SkillClient {
	return &SkillClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `skill.Hooks(f(g(h())))`.
func (c *SkillClient) Use(hooks ...Hook) {
	c.hooks.Skill = append(c.hooks.Skill, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `skill.Intercept(f(g(h())))`.
func (c *SkillClient) Intercept(interceptors ...Interceptor) {
	c.inters.Skill = append(c.inters.Skill, interceptors...)
}

// Create returns a builder for creating a Skill entity.
func (c *SkillClient) Create() *SkillCreate {
	mutation := newSkillMutation(c.config, OpCreate)
	return &SkillCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of Skill entities.
func (c *SkillClient) CreateBulk(builders ...*SkillCreate) *SkillCreateBulk {
	return &SkillCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *SkillClient) MapCreateBulk(slice any, setFunc func(*SkillCreate, int)) *SkillCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &SkillCreateBulk{err: fmt.Errorf("calling to SkillClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*SkillCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &SkillCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for Skill.
func (c *SkillClient) Update() *SkillUpdate {
	mutation := newSkillMutation(c.config, OpUpdate)
	return &SkillUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *SkillClient) UpdateOne(_m *Skill) *SkillUpdateOne {
	mutation := newSkillMutation(c.config, OpUpdateOne, withSkill(_m))
	return &SkillUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *SkillClient) UpdateOneID(id uuid.UUID) *SkillUpdateOne {
	mutation := newSkillMutation(c.config, OpUpdateOne, withSkillID(id))
	return &SkillUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for Skill.
func (c *SkillClient) Delete() *SkillDelete {
	mutation := newSkillMutation(c.config, OpDelete)
	return &SkillDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *SkillClient) DeleteOne(_m *Skill) *SkillDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *SkillClient) DeleteOneID(id uuid.UUID) *SkillDeleteOne {
	builder := c.Delete().Where(skill.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &SkillDeleteOne{builder}
}

// Query returns a query builder for Skill.
func (c *SkillClient) Query() *SkillQuery {
	return &SkillQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeSkill},
		inters: c.Interceptors(),
	}
}

// Get returns a Skill entity by its id.
func (c *SkillClient) Get(ctx context.Context, id uuid.UUID) (*Skill, error) {
	return c.Query().Where(skill.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *SkillClient) GetX(ctx context.Context, id uuid.UUID) *Skill {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// QueryFiles queries the files edge of a Skill.
func (c *SkillClient) QueryFiles(_m *Skill) *SkillFileQuery {
	query := (&SkillFileClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := _m.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(skill.Table, skill.FieldID, id),
			sqlgraph.To(skillfile.Table, skillfile.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, skill.FilesTable, skill.FilesColumn),
		)
		fromV = sqlgraph.Neighbors(_m.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// Hooks returns the client hooks.
func (c *SkillClient) Hooks() []Hook {
	return c.hooks.Skill
}

// Interceptors returns the client interceptors.
func (c *SkillClient) Interceptors() []Interceptor {
	return c.inters.Skill
}

func (c *SkillClient) mutate(ctx context.Context, m *SkillMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&SkillCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&SkillUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&SkillUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&SkillDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("team: unknown Skill mutation op: %q", m.Op())
	}
}

// SkillFileClient is a client for the SkillFile schema.
type SkillFileClient struct {
	config
}

// NewSkillFileClient returns a client for the SkillFile from the given config.
func NewSkillFileClient(c config) *SkillFileClient {
	return &SkillFileClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `skillfile.Hooks(f(g(h())))`.
func (c *SkillFileClient) Use(hooks ...Hook) {
	c.hooks.SkillFile = append(c.hooks.SkillFile, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `skillfile.Intercept(f(g(h())))`.
func (c *SkillFileClient) Intercept(interceptors ...Interceptor) {
	c.inters.SkillFile = append(c.inters.SkillFile, interceptors...)
}

// Create returns a builder for creating a SkillFile entity.
func (c *SkillFileClient) Create() *SkillFileCreate {
	mutation := newSkillFileMutation(c.config, OpCreate)
	return &SkillFileCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of SkillFile entities.
func (c *SkillFileClient) CreateBulk(builders ...*SkillFileCreate) *SkillFileCreateBulk {
	return &SkillFileCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *SkillFileClient) MapCreateBulk(slice any, setFunc func(*SkillFileCreate, int)) *SkillFileCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &SkillFileCreateBulk{err: fmt.Errorf("calling to SkillFileClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*SkillFileCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &SkillFileCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for SkillFile.
func (c *SkillFileClient) Update() *SkillFileUpdate {
	mutation := newSkillFileMutation(c.config, OpUpdate)
	return &SkillFileUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *SkillFileClient) UpdateOne(_m *SkillFile) *SkillFileUpdateOne {
	mutation := newSkillFileMutation(c.config, OpUpdateOne, withSkillFile(_m))
	return &SkillFileUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *SkillFileClient) UpdateOneID(id uuid.UUID) *SkillFileUpdateOne {
	mutation := newSkillFileMutation(c.config, OpUpdateOne, withSkillFileID(id))
	return &SkillFileUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for SkillFile.
func (c *SkillFileClient) Delete() *SkillFileDelete {
	mutation := newSkillFileMutation(c.config, OpDelete)
	return &SkillFileDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *SkillFileClient) DeleteOne(_m *SkillFile) *SkillFileDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *SkillFileClient) DeleteOneID(id uuid.UUID) *SkillFileDeleteOne {
	builder := c.Delete().Where(skillfile.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &SkillFileDeleteOne{builder}
}

// Query returns a query builder for SkillFile.
func (c *SkillFileClient) Query() *SkillFileQuery {
	return &SkillFileQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeSkillFile},
		inters: c.Interceptors(),
	}
}

// Get returns a SkillFile entity by its id.
func (c *SkillFileClient) Get(ctx context.Context, id uuid.UUID) (*SkillFile, error) {
	return c.Query().Where(skillfile.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *SkillFileClient) GetX(ctx context.Context, id uuid.UUID) *SkillFile {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// QuerySkill queries the skill edge of a SkillFile.
func (c *SkillFileClient) QuerySkill(_m *SkillFile) *SkillQuery {
	query := (&SkillClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := _m.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(skillfile.Table, skillfile.FieldID, id),
			sqlgraph.To(skill.Table, skill.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, skillfile.SkillTable, skillfile.SkillColumn),
		)
		fromV = sqlgraph.Neighbors(_m.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// Hooks returns the client hooks.
func (c *SkillFileClient) Hooks() []Hook {
	return c.hooks.SkillFile
}

// Interceptors returns the client interceptors.
func (c *SkillFileClient) Interceptors() []Interceptor {
	return c.inters.SkillFile
}

func (c *SkillFileClient) mutate(ctx context.Context, m *SkillFileMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&SkillFileCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&SkillFileUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&SkillFileUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&SkillFileDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("team: unknown SkillFile mutation op: %q", m.Op())
	}
}

// TeamAuditLogClient is a client for the TeamAuditLog schema.
type TeamAuditLogClient struct {
	config
}

// NewTeamAuditLogClient returns a client for the TeamAuditLog from the given config.
func NewTeamAuditLogClient(c config) *TeamAuditLogClient {
	return &TeamAuditLogClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `teamauditlog.Hooks(f(g(h())))`.
func (c *TeamAuditLogClient) Use(hooks ...Hook) {
	c.hooks.TeamAuditLog = append(c.hooks.TeamAuditLog, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `teamauditlog.Intercept(f(g(h())))`.
func (c *TeamAuditLogClient) Intercept(interceptors ...Interceptor) {
	c.inters.TeamAuditLog = append(c.inters.TeamAuditLog, interceptors...)
}

// Create returns a builder for creating a TeamAuditLog entity.
func (c *TeamAuditLogClient) Create() *TeamAuditLogCreate {
	mutation := newTeamAuditLogMutation(c.config, OpCreate)
	return &TeamAuditLogCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of TeamAuditLog entities.
func (c *TeamAuditLogClient) CreateBulk(builders ...*TeamAuditLogCreate) *TeamAuditLogCreateBulk {
	return &TeamAuditLogCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *TeamAuditLogClient) MapCreateBulk(slice any, setFunc func(*TeamAuditLogCreate, int)) *TeamAuditLogCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &TeamAuditLogCreateBulk{err: fmt.Errorf("calling to TeamAuditLogClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*TeamAuditLogCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &TeamAuditLogCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for TeamAuditLog.
func (c *TeamAuditLogClient) Update() *TeamAuditLogUpdate {
	mutation := newTeamAuditLogMutation(c.config, OpUpdate)
	return &TeamAuditLogUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *TeamAuditLogClient) UpdateOne(_m *TeamAuditLog) *TeamAuditLogUpdateOne {
	mutation := newTeamAuditLogMutation(c.config, OpUpdateOne, withTeamAuditLog(_m))
	return &TeamAuditLogUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *TeamAuditLogClient) UpdateOneID(id int) *TeamAuditLogUpdateOne {
	mutation := newTeamAuditLogMutation(c.config, OpUpdateOne, withTeamAuditLogID(id))
	return &TeamAuditLogUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for TeamAuditLog.
func (c *TeamAuditLogClient) Delete() *TeamAuditLogDelete {
	mutation := newTeamAuditLogMutation(c.config, OpDelete)
	return &TeamAuditLogDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *TeamAuditLogClient) DeleteOne(_m *TeamAuditLog) *TeamAuditLogDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *TeamAuditLogClient) DeleteOneID(id int) *TeamAuditLogDeleteOne {
	builder := c.Delete().Where(teamauditlog.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &TeamAuditLogDeleteOne{builder}
}

// Query returns a query builder for TeamAuditLog.
func (c *TeamAuditLogClient) Query() *TeamAuditLogQuery {
	return &TeamAuditLogQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeTeamAuditLog},
		inters: c.Interceptors(),
	}
}

// Get returns a TeamAuditLog entity by its id.
func (c *TeamAuditLogClient) Get(ctx context.Context, id int) (*TeamAuditLog, error) {
	return c.Query().Where(teamauditlog.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *TeamAuditLogClient) GetX(ctx context.Context, id int) *TeamAuditLog {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *TeamAuditLogClient) Hooks() []Hook {
	return c.hooks.TeamAuditLog
}

// Interceptors returns the client interceptors.
func (c *TeamAuditLogClient) Interceptors() []Interceptor {
	return c.inters.TeamAuditLog
}

func (c *TeamAuditLogClient) mutate(ctx context.Context, m *TeamAuditLogMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&TeamAuditLogCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&TeamAuditLogUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&TeamAuditLogUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&TeamAuditLogDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("team: unknown TeamAuditLog mutation op: %q", m.Op())
	}
}

// hooks and interceptors per client, for fast access.
type (
	hooks struct {
		App, AppState, ChatSessionEvent, Credential, DrillReport, FleetMailboxMessage,
		FleetMonitorState, FleetPlan, FleetRunState, FleetSetupDraft,
		FleetSetupProfile, FleetTask, FleetTemplate, Flow, McpServer, Memory,
		NetworkPolicy, SandboxSession, ScheduledJob, Session, SessionEvent, Setting,
		Skill, SkillFile, TeamAuditLog []ent.Hook
	}
	inters struct {
		App, AppState, ChatSessionEvent, Credential, DrillReport, FleetMailboxMessage,
		FleetMonitorState, FleetPlan, FleetRunState, FleetSetupDraft,
		FleetSetupProfile, FleetTask, FleetTemplate, Flow, McpServer, Memory,
		NetworkPolicy, SandboxSession, ScheduledJob, Session, SessionEvent, Setting,
		Skill, SkillFile, TeamAuditLog []ent.Interceptor
	}
)
