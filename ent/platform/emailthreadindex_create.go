// Code generated by ent, DO NOT EDIT.

package platform

import (
	"context"
	"errors"
	"fmt"
	"time"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/SAP/astonish/ent/platform/emailthreadindex"
)

// EmailThreadIndexCreate is the builder for creating a EmailThreadIndex entity.
type EmailThreadIndexCreate struct {
	config
	mutation *EmailThreadIndexMutation
	hooks    []Hook
}

// SetSessionKey sets the "session_key" field.
func (_c *EmailThreadIndexCreate) SetSessionKey(v string) *EmailThreadIndexCreate {
	_c.mutation.SetSessionKey(v)
	return _c
}

// SetCreatedAt sets the "created_at" field.
func (_c *EmailThreadIndexCreate) SetCreatedAt(v time.Time) *EmailThreadIndexCreate {
	_c.mutation.SetCreatedAt(v)
	return _c
}

// SetNillableCreatedAt sets the "created_at" field if the given value is not nil.
func (_c *EmailThreadIndexCreate) SetNillableCreatedAt(v *time.Time) *EmailThreadIndexCreate {
	if v != nil {
		_c.SetCreatedAt(*v)
	}
	return _c
}

// SetID sets the "id" field.
func (_c *EmailThreadIndexCreate) SetID(v string) *EmailThreadIndexCreate {
	_c.mutation.SetID(v)
	return _c
}

// Mutation returns the EmailThreadIndexMutation object of the builder.
func (_c *EmailThreadIndexCreate) Mutation() *EmailThreadIndexMutation {
	return _c.mutation
}

// Save creates the EmailThreadIndex in the database.
func (_c *EmailThreadIndexCreate) Save(ctx context.Context) (*EmailThreadIndex, error) {
	_c.defaults()
	return withHooks(ctx, _c.sqlSave, _c.mutation, _c.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (_c *EmailThreadIndexCreate) SaveX(ctx context.Context) *EmailThreadIndex {
	v, err := _c.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (_c *EmailThreadIndexCreate) Exec(ctx context.Context) error {
	_, err := _c.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (_c *EmailThreadIndexCreate) ExecX(ctx context.Context) {
	if err := _c.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (_c *EmailThreadIndexCreate) defaults() {
	if _, ok := _c.mutation.CreatedAt(); !ok {
		v := emailthreadindex.DefaultCreatedAt()
		_c.mutation.SetCreatedAt(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (_c *EmailThreadIndexCreate) check() error {
	if _, ok := _c.mutation.SessionKey(); !ok {
		return &ValidationError{Name: "session_key", err: errors.New(`platform: missing required field "EmailThreadIndex.session_key"`)}
	}
	if v, ok := _c.mutation.SessionKey(); ok {
		if err := emailthreadindex.SessionKeyValidator(v); err != nil {
			return &ValidationError{Name: "session_key", err: fmt.Errorf(`platform: validator failed for field "EmailThreadIndex.session_key": %w`, err)}
		}
	}
	switch _c.driver.Dialect() {
	case dialect.MySQL, dialect.SQLite:
		if _, ok := _c.mutation.CreatedAt(); !ok {
			return &ValidationError{Name: "created_at", err: errors.New(`platform: missing required field "EmailThreadIndex.created_at"`)}
		}
	}
	if v, ok := _c.mutation.ID(); ok {
		if err := emailthreadindex.IDValidator(v); err != nil {
			return &ValidationError{Name: "id", err: fmt.Errorf(`platform: validator failed for field "EmailThreadIndex.id": %w`, err)}
		}
	}
	return nil
}

func (_c *EmailThreadIndexCreate) sqlSave(ctx context.Context) (*EmailThreadIndex, error) {
	if err := _c.check(); err != nil {
		return nil, err
	}
	_node, _spec := _c.createSpec()
	if err := sqlgraph.CreateNode(ctx, _c.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	if _spec.ID.Value != nil {
		if id, ok := _spec.ID.Value.(string); ok {
			_node.ID = id
		} else {
			return nil, fmt.Errorf("unexpected EmailThreadIndex.ID type: %T", _spec.ID.Value)
		}
	}
	_c.mutation.id = &_node.ID
	_c.mutation.done = true
	return _node, nil
}

func (_c *EmailThreadIndexCreate) createSpec() (*EmailThreadIndex, *sqlgraph.CreateSpec) {
	var (
		_node = &EmailThreadIndex{config: _c.config}
		_spec = sqlgraph.NewCreateSpec(emailthreadindex.Table, sqlgraph.NewFieldSpec(emailthreadindex.FieldID, field.TypeString))
	)
	if id, ok := _c.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = id
	}
	if value, ok := _c.mutation.SessionKey(); ok {
		_spec.SetField(emailthreadindex.FieldSessionKey, field.TypeString, value)
		_node.SessionKey = value
	}
	if value, ok := _c.mutation.CreatedAt(); ok {
		_spec.SetField(emailthreadindex.FieldCreatedAt, field.TypeTime, value)
		_node.CreatedAt = value
	}
	return _node, _spec
}

// EmailThreadIndexCreateBulk is the builder for creating many EmailThreadIndex entities in bulk.
type EmailThreadIndexCreateBulk struct {
	config
	err      error
	builders []*EmailThreadIndexCreate
}

// Save creates the EmailThreadIndex entities in the database.
func (_c *EmailThreadIndexCreateBulk) Save(ctx context.Context) ([]*EmailThreadIndex, error) {
	if _c.err != nil {
		return nil, _c.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(_c.builders))
	nodes := make([]*EmailThreadIndex, len(_c.builders))
	mutators := make([]Mutator, len(_c.builders))
	for i := range _c.builders {
		func(i int, root context.Context) {
			builder := _c.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*EmailThreadIndexMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, _c.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, _c.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, _c.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (_c *EmailThreadIndexCreateBulk) SaveX(ctx context.Context) []*EmailThreadIndex {
	v, err := _c.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (_c *EmailThreadIndexCreateBulk) Exec(ctx context.Context) error {
	_, err := _c.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (_c *EmailThreadIndexCreateBulk) ExecX(ctx context.Context) {
	if err := _c.Exec(ctx); err != nil {
		panic(err)
	}
}
