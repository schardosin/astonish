// Code generated by ent, DO NOT EDIT.

package platform

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/schardosin/astonish/ent/platform/sandboxlayer"
)

// SandboxLayer is the model entity for the SandboxLayer schema.
type SandboxLayer struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// ParentLayer holds the value of the "parent_layer" field.
	ParentLayer *string `json:"parent_layer,omitempty"`
	// CephfsPath holds the value of the "cephfs_path" field.
	CephfsPath string `json:"cephfs_path,omitempty"`
	// SizeBytes holds the value of the "size_bytes" field.
	SizeBytes int64 `json:"size_bytes,omitempty"`
	// RefCount holds the value of the "ref_count" field.
	RefCount int `json:"ref_count,omitempty"`
	// CreatedBy holds the value of the "created_by" field.
	CreatedBy *string `json:"created_by,omitempty"`
	// AddedAt holds the value of the "added_at" field.
	AddedAt time.Time `json:"added_at,omitempty"`
	// LastReferenced holds the value of the "last_referenced" field.
	LastReferenced time.Time `json:"last_referenced,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SandboxLayerQuery when eager-loading is set.
	Edges        SandboxLayerEdges `json:"edges"`
	selectValues sql.SelectValues
}

// SandboxLayerEdges holds the relations/edges for other nodes in the graph.
type SandboxLayerEdges struct {
	// Templates holds the value of the templates edge.
	Templates []*SandboxTemplate `json:"templates,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// TemplatesOrErr returns the Templates value or an error if the edge
// was not loaded in eager-loading.
func (e SandboxLayerEdges) TemplatesOrErr() ([]*SandboxTemplate, error) {
	if e.loadedTypes[0] {
		return e.Templates, nil
	}
	return nil, &NotLoadedError{edge: "templates"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SandboxLayer) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case sandboxlayer.FieldSizeBytes, sandboxlayer.FieldRefCount:
			values[i] = new(sql.NullInt64)
		case sandboxlayer.FieldID, sandboxlayer.FieldParentLayer, sandboxlayer.FieldCephfsPath, sandboxlayer.FieldCreatedBy:
			values[i] = new(sql.NullString)
		case sandboxlayer.FieldAddedAt, sandboxlayer.FieldLastReferenced:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SandboxLayer fields.
func (_m *SandboxLayer) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case sandboxlayer.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				_m.ID = value.String
			}
		case sandboxlayer.FieldParentLayer:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field parent_layer", values[i])
			} else if value.Valid {
				_m.ParentLayer = new(string)
				*_m.ParentLayer = value.String
			}
		case sandboxlayer.FieldCephfsPath:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field cephfs_path", values[i])
			} else if value.Valid {
				_m.CephfsPath = value.String
			}
		case sandboxlayer.FieldSizeBytes:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field size_bytes", values[i])
			} else if value.Valid {
				_m.SizeBytes = value.Int64
			}
		case sandboxlayer.FieldRefCount:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field ref_count", values[i])
			} else if value.Valid {
				_m.RefCount = int(value.Int64)
			}
		case sandboxlayer.FieldCreatedBy:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field created_by", values[i])
			} else if value.Valid {
				_m.CreatedBy = new(string)
				*_m.CreatedBy = value.String
			}
		case sandboxlayer.FieldAddedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field added_at", values[i])
			} else if value.Valid {
				_m.AddedAt = value.Time
			}
		case sandboxlayer.FieldLastReferenced:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_referenced", values[i])
			} else if value.Valid {
				_m.LastReferenced = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SandboxLayer.
// This includes values selected through modifiers, order, etc.
func (_m *SandboxLayer) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryTemplates queries the "templates" edge of the SandboxLayer entity.
func (_m *SandboxLayer) QueryTemplates() *SandboxTemplateQuery {
	return NewSandboxLayerClient(_m.config).QueryTemplates(_m)
}

// Update returns a builder for updating this SandboxLayer.
// Note that you need to call SandboxLayer.Unwrap() before calling this method if this SandboxLayer
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *SandboxLayer) Update() *SandboxLayerUpdateOne {
	return NewSandboxLayerClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the SandboxLayer entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *SandboxLayer) Unwrap() *SandboxLayer {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("platform: SandboxLayer is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *SandboxLayer) String() string {
	var builder strings.Builder
	builder.WriteString("SandboxLayer(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	if v := _m.ParentLayer; v != nil {
		builder.WriteString("parent_layer=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	builder.WriteString("cephfs_path=")
	builder.WriteString(_m.CephfsPath)
	builder.WriteString(", ")
	builder.WriteString("size_bytes=")
	builder.WriteString(fmt.Sprintf("%v", _m.SizeBytes))
	builder.WriteString(", ")
	builder.WriteString("ref_count=")
	builder.WriteString(fmt.Sprintf("%v", _m.RefCount))
	builder.WriteString(", ")
	if v := _m.CreatedBy; v != nil {
		builder.WriteString("created_by=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	builder.WriteString("added_at=")
	builder.WriteString(_m.AddedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("last_referenced=")
	builder.WriteString(_m.LastReferenced.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// SandboxLayers is a parsable slice of SandboxLayer.
type SandboxLayers []*SandboxLayer
