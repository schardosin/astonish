grep result:
----------------
$(grep -n 'G124' .golangci.yml || true)

golangci-lint config verify output:
----------------------------------
$(set -o pipefail && if command -v golangci-lint >/dev/null 2>&1; then golangci-lint config verify 2>&1 || true; else docker run --rm -v "$PWD":/app -w /app golangci/golangci-lint:v2.11.4 golangci-lint config verify 2>&1 || true; fi)

Note: If the file above is empty for grep result and linter output shows no errors, the configuration is valid.
